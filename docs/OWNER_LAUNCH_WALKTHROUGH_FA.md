# مسیر همراهی صاحب پروژه: بازبینی دستی، یادگیری و لانچ

آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۲۱

## هدف و نقطهٔ ادامه

**هدف اصلی طبق توضیح تکمیلی صاحب پروژه در ۲۰۲۶-۰۹-۲۰: یادگیری جامع و باحوصلهٔ
همین پروژه، همراه با بازبینی و اصلاح آن، به‌ویژه فرانت‌اند.** صاحب پروژه می‌خواهد
بداند چه ساخته شده، چه کارهایی انجام شده، در چه مرحله‌ای هستیم و هر سرویس،
فناوری، فایل، متغیر و تصمیم پیاده‌سازی چه نقشی دارد. لانچ مقصد مسیر است؛
چک‌لیست انتشار نباید آموزش را به چند مسیر اصلی کاربر محدود کند.

**نقطهٔ ادامه: جلسهٔ ۱، سربرگ و تب‌های پنل ادمین روی staging اصلاح و تأیید شدند. فایل‌های مصنوعیِ درس نمونه به‌صورت محلی آماده‌اند. مسیر آپلود مستقیم تصویر/ویدیو در پنل پیاده‌سازی و تست فنی شده؛ تا تأیید استقرار و بارگذاری نمونه، صفحهٔ دوره/پلیر در staging بازبینی نشده است.**
تصویر کلی و وضعیت موجود توضیح داده می‌شود، سپس صفحه‌ها با صاحب پروژه دیده
و از ظاهر تا کد و داده دنبال می‌شوند. سه اولویت محصول قبلی و مسیر حساب حفظ
می‌شوند، اما سقف دامنهٔ یادگیری نیستند. دریافت دستگاه‌ها یا ورود staging
پیش‌نیاز ادامهٔ توضیح مفاهیم نیست. صاحب پروژه تصویر هر دو تب خانه و صفحه‌های
کاوش/HSK در staging را فرستاده است؛ سایر صفحه‌ها هنوز دیده نشده‌اند. هر نوبت یک موضوع کوچک و حداکثر
۳ تا ۵ اقدام مشخص دارد.

## وضعیت مبنا و شواهد

- پروژه: `E:/Chinverse`؛ شاخه: `codex/phase-8-beta-release`.
- HEAD هنگام تهیهٔ این راهنما: `b90c957b208d4e423eb3ebe15d21eafcb9ae62fc`.
- release backend مشاهده‌شده در همین نوبت:
  `54973b82341924f5056ba1d8d3cbb108c5c166ab`.
- `GET /health`: پاسخ `200`، tier=`staging` و `indexable=false`.
- `GET /health/ready`: پاسخ `200` و database_target/database/storage همگی `ok`.
- `GET /api/v1/courses/?limit=1`: پاسخ عمومی `200 []`؛ این فقط خالی‌بودن
  کاتالوگ قابل‌مشاهدهٔ این درخواست را ثابت می‌کند، نه نبود همهٔ draftها در DB.
- health فرانت‌اند بدون نشست Vercel پاسخ `302` داد؛ سلامت داخلی frontend و
  تطابق SHA آن در این نوبت دوباره تأیید نشده‌اند.
- مرحلهٔ ۷ قبلاً برای دامنهٔ غیرعمومی با `CLOSED-PREPUBLIC` بسته شد.
  آمادگی عمومی باید دوباره بررسی شود؛ waiverهای قبلی اثبات آمادگی عمومی نیستند.
- پرداخت فعلاً رایگان و ارسال واقعی email/SMS خارج از دامنهٔ فعال است؛ سیاست
  ورود، تأیید حساب و بازیابی رمز نسخهٔ عمومی هنوز باید مشخص و آزموده شود.
- تست واقعی Android/iOS و دسترس‌پذیری در گزارش مرحلهٔ ۴ همچنان deferred است.
- صفرهای issue inventory مربوط به snapshot قبلی هستند؛ این نوبت query تازه‌ای
  از issue tracker انجام نشده است.
- جدول‌های آغاز برنامهٔ قدیمی و `PROJECT_HANDOFF_FA.md` بخشی از تاریخچه‌اند؛
  بعضی SHAها و URLهای آن‌ها قدیمی است. برای تصمیم، شواهد تاریخ‌دار جدید را ملاک بگیر.
- فایل‌های موجود و untrackedِ `frontend/AGENTS.md` و `frontend/CLAUDE.md`
  متعلق به این کار نیستند؛ بدون بررسی با تغییرات این مسیر مخلوط نشوند.

این راهنما تصمیم یا عملی برای انتشار عمومی ثبت نمی‌کند. اقدام‌های این نوبت
خواندن کد، probeهای GET و نوشتن راهنمای ادامه بوده‌اند.

## روش همکاری و آموزش

اصل مسیر: «تصویر کلی → یک بخش واقعی محصول → کد و علت رفتار → مشاهده و اصلاح
→ بازآزمایی → ثبت یادگیری و نقطهٔ ادامه». توضیح از نقش سادهٔ هر چیز شروع شود؛
اصطلاح تخصصی پس از مثال معرفی شود. مثال‌های صاحب پروژه (دیتابیس، Vercel،
staging، GitHub، Sentry، Docker و variable) نمونه‌اند و فهرست بستهٔ آموزش نیستند.

دلیل تاریخی انتخاب یک فناوری را فقط با سند یا تاریخچه بیان کن. وقتی فقط
کاربرد فعلی و مزیت عملی آن از کد روشن است، همین را توضیح بده و برای انگیزهٔ
اولیه داستان نساز. «در کد وجود دارد»، «تست فنی دارد»، «صاحب پروژه دیده»، و
«فهم صاحب پروژه تأیید شده» چهار وضعیت جدا هستند.

۱. پیش از هر کار بگو «این بخش چیست، چه مشکلی حل می‌کند، چرا الان سراغش می‌رویم».
۲. حداکثر ۳ تا ۵ دستور کوتاه با مسیر دقیق صفحه و نتیجهٔ مورد انتظار بده.
۳. کارهای دیداری ساده و تصمیم محصول را صاحب پروژه انجام دهد؛ عیب‌یابی کد،
   تست و اصلاحات فنی را دستیار انجام دهد و توضیح دهد.
۴. هر ایراد در [فهرست بازبینی](OWNER_LAUNCH_REVIEW_LOG_FA.md) شناسه بگیرد.
   «مشاهده‌شده»، «از کد پیدا شده» و «هنوز نیازمند آزمایش» را جدا ثبت کن.
۵. اصلاح مرتبط را با تست متناسب بررسی کن؛ نتیجهٔ خودکار و تأیید چشمی صاحب
   پروژه دو ستون جدا هستند. build سبز به‌معنی تأیید تجربهٔ کاربر نیست.
۶. آموزش فقط به وجود باگ وابسته نیست: ساختار صفحه، فایل، متغیر، درخواست و
   علت پیاده‌سازی را هم توضیح بده. تمرین کوچک در صورت مفیدبودن پیشنهاد شود؛
   صرف ارائهٔ توضیح به‌معنی تسلط صاحب پروژه نیست و جلسه نباید حالت امتحان بگیرد.
۷. پس از هر بخش، این فایل و فهرست بازبینی را با نتیجه و اقدام بعدی به‌روز کن.
   تغییر حساب یا پایان توکن نباید باعث تکرار کار انجام‌شده شود.

اگر کار فنی مستقلی قابل انجام است، منتظر پاسخ کارهای دستی نمان. اما مشاهدهٔ
انجام‌نشده را pass نزن و انتخاب‌های محصول را از طرف صاحب پروژه قطعی نکن.
تغییرات را در بسته‌های کوچک و قابل‌فهم نگه دار و بازطراحی گسترده را به نام رفع
یک ایراد جزئی انجام نده. رمز، کد MFA، cookie و secret در گزارش ثبت نشوند.

## نقشهٔ سادهٔ پروژه برای جلسهٔ ۱

| بخش | مسئولیت | جای آن در پروژه / سرویس |
|---|---|---|
| ظاهر و تعامل صفحه | متن، دکمه، فرم، نمایش خطا و حرکت بین صفحه‌ها | Next.js/React، `frontend/src/app` و `frontend/src/components`؛ میزبانی Vercel |
| ارتباط صفحه با سرور | درخواست‌های HTTP مرورگر به `/api/backend` و انتقال به backend | `frontend/src/lib/api.ts` و `frontend/src/app/api/backend/[...path]/route.ts` |
| منطق و مجوزها | ورود، مالکیت، چت، آموزش و دسترسی ادمین | FastAPI، `backend/app/main.py` و `backend/app/api/v1/endpoints`؛ HF staging |
| داده‌های ماندگار | حساب‌ها، دوره‌ها، پیام‌ها و بازخوردها | PostgreSQL/Neon؛ مدل‌ها در `backend/app/models` |
| تغییر ساختار DB | به‌روزرسانی نسخهٔ جدول‌ها به‌صورت قابل‌ردیابی | Alembic و `backend/alembic` |
| فایل و رسانه | تصویر، ویدیو و زیرنویس؛ مقصد واقعی از تنظیمات همان محیط تعیین می‌شود | `backend/app/core/storage.py`؛ تنظیمات storage در جلسهٔ رسانه بررسی شود |
| نسخه و استقرار | Git تغییرات را نگه می‌دارد؛ GitHub Actions تست می‌کند؛ deploy نسخه را روی سرویس می‌گذارد | `.github/workflows` |
| مشاهدهٔ خطا | یافتن خطاهای واقعی و ارتباطشان با نسخه | Sentry و `backend/app/core/observability.py` |

مثال آموزشی: بازکردن فهرست دوره‌ها در مرورگر → درخواست به `/api/backend` →
backend → خواندن DB → پاسخ → نمایش کارت‌ها. ویدیو فایل جدا دارد؛ صرف دیدن
عنوان درس، کارکرد پخش و مجوز دسترسی را ثابت نمی‌کند. چت زنده مسیر WebSocket
هم دارد و فقط با بررسی HTTP پوشش داده نمی‌شود.

local یعنی نسخهٔ روی رایانه برای توسعه؛ staging نسخهٔ آزمایشی روی اینترنت است؛
production نسخهٔ کاربران واقعی است. branch مسیر تغییرات است، commit شناسهٔ یک
نسخه از کد، و deploy اجرای آن نسخه روی سرویس است. این‌ها ممکن است یک SHA نداشته باشند.

### درس آغازین: نقش ابزارها در همین پروژه

- محصول: وب‌اپ فارسی برای یادگیری زبان چینی و ارتباط/معرفی حرفه‌ای؛ علاوه بر
  درس و لایتنر، پروفایل، شبکه، خدمات/گالری، گفتگو، پشتیبانی و مدیریت در کد وجود دارند.
- React اجزای تعاملی رابط را می‌سازد و Next.js چارچوب صفحه‌ها و اجرای فرانت است.
  TypeScript به مشخص‌کردن نوع داده‌ها کمک می‌کند و Tailwind برای استایل‌دهی است.
  این توضیح نقش فعلی آن‌هاست؛ انگیزهٔ تاریخی انتخاب اولیه جداگانه بررسی می‌شود.
- Vercel (ورسل) میزبان فرانت اینترنتی است. مرورگر از آن کد و صفحه می‌گیرد؛
  پردازش و مجوزهای اصلی حساب/محتوا در backend مستقل FastAPI انجام می‌شوند.
- Hugging Face Space میزبان backend آزمایشی است. استفاده از این سرویس در این
  پروژه به‌خودی‌خود به معنی وجود مدل هوش مصنوعی در همهٔ قابلیت‌ها نیست.
- PostgreSQL نوع پایگاه داده است؛ Neon سرویس میزبانی آن است. گزارش استقرار
  مرحلهٔ ۲ شاخهٔ `staging` را ثبت کرده؛ پنل و داده‌های فعلی در این بخش باز نشده‌اند.
- Git تاریخچهٔ تغییر کد را ثبت می‌کند؛ GitHub مخزن آنلاین و محل همکاری است؛
  GitHub Actions دستورهای تست و استقرار را طبق workflow اجرا می‌کند.
  workflowهای فعلی در `.github/workflows` هستند و triggerها و شرط‌های خودشان را دارند.
- Docker در `backend/Dockerfile` محیط Python و وابستگی‌های backend را بسته‌بندی
  می‌کند. دستور شروع فعلی migration را اجرا و سپس Uvicorn را برای سرویس FastAPI
  راه‌اندازی می‌کند. جزئیات image/container، port و migration در بخش زیرساخت باز می‌شوند.
- Sentry خطاهای برنامه را برای پیگیری به نسخه و محیط مرتبط می‌کند؛ اصلاح کد
  را خودکار انجام نمی‌دهد. شواهد فعال‌سازی زندهٔ staging در گزارش عملیات مرحلهٔ ۳
  ثبت شده‌اند؛ در این بخش بازدید تازه‌ای از پنل Sentry انجام نشده است.
- رسانه از رکوردهای DB جداست. از وجود پشتیبانی `s3` در کد نمی‌توان provider
  جاری را نتیجه گرفت؛ mounted storage و S3 هر دو در کد پشتیبانی می‌شوند و
  مقصد واقعی در درس ذخیره‌سازی از تنظیمات همان محیط بررسی می‌شود.

منابع این توضیح: `README.md`، `frontend/package.json`، `backend/pyproject.toml`،
`backend/Dockerfile`، `backend/app/db/session.py`، `backend/app/core/storage.py`،
`backend/app/core/observability.py` و گزارش‌های استقرار/عملیات.

### از کجا وارد کد فرانت می‌شویم؟

اولین صفحهٔ انتخاب‌شده برای آموزش، خانه در `frontend/src/app/page.tsx` است.
در کد محلی دو تب «فعالیت‌ها» و «روند یادگیری» وجود دارد. متغیر `activeTab`
نگه می‌دارد کدام تب انتخاب شده است؛ `setActiveTab` آن را تغییر می‌دهد و React
نمایش را بر اساس مقدار تازه به‌روز می‌کند. این state در همین صفحه است و
به‌خودی‌خود رکوردی در دیتابیس نمی‌سازد. مقدار اولیه `activities` است و پارامتر
معتبر `tab` در URL هم در این کد می‌تواند تب شروع را تعیین کند.

در همین فایل، `loadFeed` با `api.get("/feed")` دادهٔ فعالیت‌ها را می‌گیرد؛
پس تعویض تب و دریافت داده دو کار قابل‌تفکیک‌اند. این فقط خواندن کد محلی است؛
نمایش دو تب و حالت خالی فعالیت‌ها در تصویر staging با این ساختار سازگار است؛
درخواست Network همان مرورگر هنوز مشاهده نشده است.

بخش بعد: مشاهدهٔ خانه، تشخیص قاب مشترک و محتوای صفحه، سپس متن/فاصله/رنگ و
رفتار تب‌ها؛ در صورت ایراد، همان بخش کد اصلاح و با تست متناسب بازآزمایی شود.

### فهرست پوشش یادگیری جامع

| موضوع | مسیر یادگیری در خود پروژه | وضعیت آموزشی |
|---|---|---|
| محصول و سابقهٔ کار | قابلیت‌ها، فازهای قبلی، کار انجام‌شده و کار باقی‌مانده | معرفی آغازین؛ مرور جزئیات باقی است |
| فرانت‌اند | خانه، قاب مشترک، مسیرها، component، state/variable، استایل و RTL؛ سپس همهٔ صفحه‌ها | اولویت شروع؛ یک مثال `activeTab` معرفی شد |
| حساب و امنیت | ثبت‌نام/ورود، session، cookie/token، تأیید/بازیابی، نقش‌ها و MFA | باقی است؛ `REV-002` باز |
| ارتباط با سرور | API، مسیر واسط `/api/backend`، درخواست/پاسخ، خطا، cache و WebSocket | نقشهٔ کلی معرفی شد؛ ردیابی عملی باقی است |
| دیتابیس | Neon/PostgreSQL، جدول‌ها و روابط، مدل‌ها، SQLAlchemy، Alembic، backup/restore | نام و نقش معرفی شد؛ مشاهده و کار عملی باقی است |
| محتوا و فایل | درس، زیرنویس، پلیر، انتشار، storage، آدرس فایل و مجوز پخش | باقی است؛ `REV-001` باز |
| Git و GitHub | branch، commit، diff، push، PR، Actions، تست و deploy | نقش کلی معرفی شد؛ یک چرخهٔ واقعی باقی است |
| اجرا و زیرساخت | local/staging/production، Vercel/HF، Docker، متغیرهای محیطی/secret، دامنه و تنظیم سرور | نقش کلی معرفی شد؛ جزئیات باقی است |
| کیفیت و عملیات | تست‌ها، Sentry، لاگ، monitor، خطایابی، عملکرد، rollback و رسیدگی به اختلال | نقش Sentry معرفی شد؛ تمرین عملی باقی است |
| مدیریت و استفادهٔ روزانه | پنل ادمین، محتوا، کاربران، پشتیبانی، moderation و feature flagها | باقی است |
| دستگاه و انتشار | موبایل/PWA/دسترس‌پذیری، نسخهٔ آزموده، gateها و انتشار | باقی است؛ `REV-003` و `REV-004` باز |

این جدول قابل گسترش است. «معرفی شد» فقط ارائهٔ توضیح را ثبت می‌کند؛ تأیید فهم
یا مشاهدهٔ موفق رابط نیست. پرسش دربارهٔ هر متغیر یا تصمیم، در همان بخش مرتبط
پاسخ داده شود و موضوع‌های ناتمام به این فهرست اضافه شوند.

## مسیر جلسه‌ها

جدول زیر پوشش بازبینی تا لانچ را نگه می‌دارد و ترتیب سخت آموزش نیست. طبق
خواستهٔ تازهٔ صاحب پروژه، پس از معرفی کلی ابتدا خانه و فرانت را باز می‌کنیم؛
مفاهیم زیرساختی هنگام رسیدن به نمونهٔ واقعی‌شان توضیح داده می‌شوند. دریافت
مشخصات دستگاه یا تعیین مجدد سه مسیر نباید شروع آموزش را متوقف کند.

| جلسه | با هم چه بررسی می‌کنیم؟ | چه چیزی یاد می‌گیری؟ | خروجی |
|---|---|---|---|
| ۱ | دامنهٔ نسخهٔ اول، دستگاه‌ها، محیط تست و اولین بازدید | frontend/backend/DB، staging و commit/deploy | سه مسیر مهم نسخهٔ اول، آدرس و نسخهٔ تست، اولین مشاهده‌ها |
| ۲ | ثبت‌نام، ورود، خروج، تأیید حساب، بازیابی رمز، MFA ادمین | session، نقش کاربر، درخواست Network و تفاوت 401/403 | مسیر ورود قابل‌استفاده با سیاست واقعی delivery |
| ۳ | خانه، کشف، دوره، درس، پلیر، زیرنویس، لایتنر و هدف روزانه | محتوا در DB، فایل، انتشار درس و مجوز پخش | یک مسیر آموزشی کامل با محتوای نمونه و بررسی ظاهر |
| ۴ | پروفایل، شبکه، ویترین، گالری، خدمات، feed، تالار، چت و اعلان | مالکیت، cache، ارتباط زنده و تست با دو حساب | journeyهای در دامنهٔ لانچ با کاربر عادی A/B تأیید شوند |
| ۵ | تنظیمات، پشتیبانی، گزارش/block، moderation و مدیریت محتوا/بتا | کار روزانهٔ ادمین، triage و مدیریت داده | صاحب پروژه یک عملیات واقعی پشتیبانی و محتوا انجام دهد |
| ۶ | Android/iOS واقعی، keyboard، back، fullscreen، dark، zoom، PWA و دسترس‌پذیری | فرق شبیه‌ساز و دستگاه، cache/offline و نسخهٔ PWA | نتیجهٔ هر دستگاه و ایرادهای باقی‌مانده ثبت شود |
| ۷ | تنظیمات نسخهٔ عمومی، دامنه، DB/storage، ورود/بازیابی، مانیتور و gateها | env/secret، backup/restore، مشاهدهٔ خطا و rollback | معافیت‌های قبلی بازبینی و مسیر عمومی قابل‌اجرا شود |
| ۸ | مرور مجدد مسیرهای اصلی و تمرین انتشار، سپس انتشار محدود | تصمیم انتشار بر اساس شواهد و رسیدگی به خطا | نسخهٔ تأییدشده، بازگشت آماده و نتیجهٔ مشاهدهٔ کاربران اولیه |

جلسه به معنی یک پیام یا یک روز نیست؛ هر جلسه چند نوبت کوتاه دارد. اولویت مسیرها
پس از تعیین سه کار مهم نسخهٔ اول تنظیم شود. قابلیت‌های خارج از نسخهٔ اول صریحاً
ثبت شوند و رابط اپ هم وضعیت آن‌ها را درست نشان دهد. دعوت/امتیاز/اشتراک با توجه
به feature flag واقعی بررسی شوند، نه صرف وجود فایل صفحه.

## جلسهٔ ۱، نوبت اول — اقدام‌های کوچک

- [x] دستیار branch/HEAD، health/readiness، کاتالوگ عمومی و دسترسی ناشناس Preview را بررسی کرد؛ در ۲۰۲۶-۰۹-۲۰، ساعت ۱۰:۰۲ UTC دوباره بررسی شد.
- [x] نقشهٔ frontend/backend/DB و تفاوت local/staging/deploy توضیح داده شد.
- [x] صاحب پروژه سه مسیر درس، لایتنر و پروفایل/ارتباط را تأیید کرد و خواست ثبت‌نام و ورود هم لحاظ شوند.
- [ ] دستگاه شروع و دسترسی به Android/iPhone مشخص شود؛ نبود دستگاه «تست‌نشده» است.
- [x] صاحب پروژه آدرس staging را در Chrome دسکتاپ باز کرد؛ عنوان تب «چین‌ورس» و صفحهٔ خانه دیده شد.
- [x] تفاوت ورود Vercel و ورود داخل Chinverse توضیح داده شد.
- [ ] نسخهٔ frontend از `/api/health` در نشست مجاز خوانده و با backend و نسخهٔ محلی مقایسه شود.
- [x] صاحب پروژه تصویر اولیهٔ خانه را فرستاد؛ تب «فعالیت‌ها» و حالت خالی دیده شد.
- [ ] تب «روند یادگیری» و مسیر ورود/پروفایل در نوبت‌های بعد دیده شوند؛ هر ایراد یا پیشنهاد ثبت شود.

آدرس staging ثبت‌شده در workflow:
[Chinverse staging](https://chinverse-git-codex-phase-8-beta-release-death-stroke.vercel.app/)

اگر صفحهٔ ورود Vercel دیده شد، این درِ محیط آزمایشی است، نه فرم ورود Chinverse.
پس از ورود مجاز، بازبینی معمول محصول با حساب کاربر عادی انجام شود. ادمین فقط
برای کارهای مدیریتی استفاده شود؛ موفقیت ادمین، سلامت مسیر کاربر جدید را ثابت نمی‌کند.
پیش از ساخت حساب جدید، مورد `REV-002` بررسی شود تا مسیر تأیید حساب روشن باشد.

تمرین کوتاه پایان نوبت: صاحب پروژه با زبان خودش توضیح دهد صفحه از کجا داده
می‌گیرد و چرا تغییر کد روی رایانه فوراً همان تغییر در سایت نیست.

### تصمیم دامنه و دستگاه‌ها — جلسهٔ ۱

صاحب پروژه پیشنهاد سه مسیر زیر را تأیید کرد و افزود: «لاگین و ثبت نام و اینا
هم در نظر بگیر». مسیر حساب پیش‌نیاز مشترک این سه کار است و پیش از آزمون کامل
آن‌ها بررسی می‌شود؛ سیاست تأیید حساب و delivery هنوز تصمیم‌گیری نشده است.

| مسیر تأییدشده | نقطهٔ شروع در کد فعلی | نتیجه‌ای که در بازبینی دنبال می‌کنیم |
|---|---|---|
| پیدا کردن و تماشای درس چینی | `/explore` و صفحهٔ درس/پخش مرتبط | کاربر عادی یک محتوای قابل‌دسترسی پیدا کند و درس را ببیند |
| ذخیره و مرور واژه‌ها در لایتنر | `/leitner` و `/leitner/review` | واژه ذخیره و مرور شود و نتیجه پس از refresh باقی بماند |
| ساخت پروفایل و ارتباط با دیگران | `/profile`، `/profile/network` و مسیر ارتباط منتخب | پروفایل ذخیره شود و ارتباط با حساب دوم آزموده شود؛ جزئیات تعامل در جلسهٔ ۴ مشخص می‌شود |
| پیش‌نیاز مشترک: حساب | `/signup`، `/login`، `/verify-account`، `/forgot-password` و خروج | ثبت‌نام، ورود، خروج و بازیابی با سیاست واقعی نسخهٔ اول هماهنگ و قابل‌آزمون باشند |

وجود این مسیرها در کد، اثبات کارکرد آن‌ها در staging نیست. هیچ قابلیت دیگری
صرفاً با این اولویت‌بندی خاموش یا از دامنهٔ محصول حذف نشده است.

- دستگاه شروع و مرورگر: پرسیده شده؛ منتظر پاسخ صاحب پروژه.
- Android و iPhone واقعی، مدل و سیستم‌عامل: پرسیده شده؛ هنوز اعلام نشده‌اند.
- تا دریافت پاسخ، دسترسی یا نبود دسترسی به دستگاه فرض نمی‌شود؛ `REV-003` باز است.
- نخستین مشاهدهٔ صاحب پروژه از staging در ۲۰۲۶-۰۹-۲۱ دریافت شد؛ این خط
  وضعیت قدیمی همین چک‌لیست را به‌روز می‌کند، جزئیات در checkpoint جدید است.

## روش بررسی هر بخش

برای مسیرهای داخل دامنهٔ لانچ، در چند نوبت کوتاه این حالت‌ها را پوشش بده:

- کار عادی از ابتدا تا نتیجه، سپس refresh و بازگشت مرورگر؛ دادهٔ ذخیره‌شده باقی بماند.
- کاربر تازه، کاربر دارای داده، حالت خالی و در مسیرهای مالکیت‌دار حساب A و B.
- ورودی ناقص/اشتباه، بارگذاری، قطع و وصل شبکه و دوبار زدن دکمهٔ ارسال.
- متن فارسی، چینش RTL، خوانایی، تضاد رنگ، اندازهٔ دکمه و پیام خطای قابل‌فهم.
- اتصال نتیجهٔ صفحه به پاسخ API/داده و در صورت نیاز لاگ؛ ظاهر به‌تنهایی کافی نیست.

برای جلسهٔ رسانه حداقل یک درس مصنوعی کوتاه و رایگان با روش انتشار پشتیبانی‌شده
بساز؛ شناسه و برنامهٔ cleanup آن ثبت شود. نتیجهٔ قدیمی signed playback پس از
حذف fixture، جای تست تجربهٔ فعلی روی دستگاه را نمی‌گیرد.

## شرط رسیدن به انتشار

انتشار عمومی زمانی بررسی شود که مسیرهای انتخابی نسخهٔ اول توسط صاحب پروژه
تأیید شده‌اند، P0/P1 باز نداریم، موارد تست‌نشده شفاف‌اند و نسخهٔ مورد آزمون
با نسخهٔ انتشار یکی است. داده و فایل production، health، راه برگشت و مسیر
گزارش خطا باید قابل‌استفاده باشند. صاحب پروژه باید بتواند نسخه را تشخیص دهد،
یک خطای گزارش‌شده را پیدا کند و بداند هنگام اختلال چه اقدامی لازم است.

SMS الزام مستقل همهٔ محصولات نیست. اما اگر ثبت‌نام یا بازیابی حساب کد می‌خواهد،
باید کانال واقعی یا روش جایگزینِ صریح و آزموده‌شده داشته باشد. پرداخت تا تصمیم
لانچ پولی خاموش می‌ماند. قبل از public، مورد `REV-004` دربارهٔ اعتبار waiverها
در gate رفع شود. تأیید عمومی بعد از مرور نسخه و شواهد مشخص انجام می‌شود.

## checkpoint هر نوبت

```text
تاریخ:
جلسه / نوبت:
محیط، URL، branch و SHAهای frontend/backend:
کار انجام‌شده توسط صاحب پروژه:
مفهوم آموزش‌داده‌شده و تمرین:
مشاهده‌ها و شناسه‌های REV:
اصلاح کد / commit / deploy (اگر انجام شد):
تست خودکار:
تأیید چشمی صاحب پروژه:
کار باز و علت:
اقدام بعدی صاحب پروژه (حداکثر ۳ مورد):
اقدام بعدی دستیار:
```

### checkpoint آماده‌سازی — ۲۰۲۶-۰۹-۲۰

راهنما و دفتر بازبینی ساخته شد. وضعیت local و probeهای زنده در ابتدای این فایل
ثبت شده‌اند. چهار مورد `REV-001` تا `REV-004` برای ادامه ثبت شد. بازبینی چشمی
جدید، تغییر runtime، deploy یا انتشار عمومی در این نوبت انجام نشد. قدم بعدی
جلسهٔ ۱، نوبت اول است.

### checkpoint جلسهٔ ۱، نوبت اول — ۲۰۲۶-۰۹-۲۰

- محیط: staging؛ نشانی frontend همان لینک بالا و backend برابر
  `https://moamin9-chinverse-api.hf.space` است. نشانی‌ها با workflow
  `.github/workflows/phase2-staging-smoke.yml` تطبیق داده شدند.
- branch محلی: `codex/phase-8-beta-release`؛ HEAD محلی:
  `b90c957b208d4e423eb3ebe15d21eafcb9ae62fc`.
- GETهای تازه در بازهٔ `2026-09-20T10:02:33Z` تا `10:02:35Z`
  (حدود ۱۳:۳۲ به وقت ایران): backend `/health` و `/health/ready` هر دو `200`؛
  tier=`staging`، `indexable=false`؛ database_target/database/storage همگی `ok`.
- release backend: `54973b82341924f5056ba1d8d3cbb108c5c166ab`؛ با HEAD
  محلی یکسان نیست. علت تفاوت در این نوبت بررسی نشده است؛ اجرای HEAD محلی
  روی staging ادعا نمی‌شود.
- کاتالوگ عمومی `/api/v1/courses/?limit=1`: پاسخ `200 []`؛ `REV-001`
  همچنان باز است. draftهای احتمالی بررسی نشده‌اند.
- GET ناشناس frontend `/` و `/api/health`: هر دو `302` به میزبان
  `vercel.com`. SHA frontend و تجربهٔ داخل سایت هنوز تأیید نشده‌اند.
  redirect محافظ محیط، به‌خودی‌خود باگ محصول ثبت نشده است.
- کار صاحب پروژه: تأیید سه کار اصلی و تأکید بر ثبت‌نام و ورود؛ پاسخ دستگاه‌ها
  و مشاهدهٔ چشمی هنوز دریافت نشده است.
- آموزش: صفحه درخواست می‌فرستد، backend منطق و مجوز را اعمال می‌کند و DB
  داده را نگه می‌دارد؛ فایل رسانه جداست؛ تغییر local برای دیده‌شدن در staging
  به deploy نیاز دارد. تفاوت ورود Vercel و Chinverse توضیح داده شد.
- تمرین فهم مطلب: پاسخ صاحب پروژه هنوز دریافت نشده است.
- بررسی فنی: GETهای فقط‌خواندنی و خواندن مسیرهای صفحه/API؛ تغییر کد برنامه،
  commit یا deploy انجام نشده و اجرای مجموعه‌تست جدید لازم نبوده است.
- تأیید چشمی صاحب پروژه: هیچ مسیر محصول هنوز pass نشده است.
- تغییر گزارش: همین راهنما و دفتر بازبینی؛ تغییرات موجود سایر فایل‌ها حفظ شد.
- کار باز: دستگاه‌ها، ورود مجاز به Preview، خواندن SHA frontend و اولین مشاهده؛
  سیاست ورود/تأیید/بازیابی `REV-002` همچنان باز است.
- اقدام بعدی صاحب پروژه: مشخص‌کردن دستگاه‌ها؛ بازکردن لینک staging و اعلام
  نام صفحه و دامنه/مسیر بدون پارامترهای ورود. اگر Vercel دیده شد، آن را به‌عنوان
  درِ محیط آزمایشی گزارش کند. ثبت‌نام آزمایشی پس از روشن‌شدن مسیر `REV-002` است.
- اقدام بعدی دستیار: ثبت دستگاه‌ها و مشاهده، بررسی نسخهٔ frontend در نشست
  مجاز، سپس دادن چند اقدام کوتاه برای بازدید خانه و فرم ورود.

### checkpoint اصلاح مسیر آموزش — ۲۰۲۶-۰۹-۲۰

- توضیح صاحب پروژه: هدف اصلی فهم جامع تمام کارهای انجام‌شده، فناوری‌ها،
  سرویس‌ها، کد و وضعیت پروژه است؛ بازبینی و اصلاح، به‌ویژه فرانت، هم‌زمان انجام شود.
- تصمیم: آموزش از تصویر کلی آغاز شود و سپس با صفحهٔ خانه وارد جزئیات شویم؛
  تمرکز صرف بر چک‌لیست لانچ و پرسش دستگاه‌ها کنار گذاشته شد. سه مسیر محصول
  قبلی حفظ شده‌اند و محدودهٔ کل آموزش را تعیین نمی‌کنند.
- بررسی انجام‌شده: کد و پکیج‌های فرانت، layout/home، اتصال DB، Dockerfile،
  storage/observability، workflowها و گزارش‌های فاز صفر، استقرار، عملیات و مرحلهٔ ۷.
- شفاف‌سازی نسخه: مقایسهٔ Git از release backend مشاهده‌شدهٔ نوبت قبل
  `54973b8` تا HEAD محلی `b90c957` فقط تغییر چهار فایل گزارش/evidence را نشان داد؛
  در این بازه کد برنامه تغییر نکرده است. این مقایسه SHA frontend زنده را تعیین نمی‌کند.
- شواهد آنلاین جدید: در این بخش GET یا بازدید پنل تازه انجام نشد؛ وضعیت زندهٔ
  ذکرشده متعلق به probeهای نوبت اول، ساعت ۱۰:۰۲ UTC است.
- مطالب آغازین: محصول و نقش بخش‌ها، جای سرویس‌ها، مفهوم محیط‌ها و معرفی
  `activeTab` در خانه. ریز همهٔ تصمیم‌های تاریخی هنوز ممیزی نشده است.
- تغییر برنامه/commit/deploy: انجام نشده؛ فقط دو گزارش آموزشی به‌روز شدند.
- نتیجهٔ تست و مشاهده: خواندن کد و مقایسهٔ Git؛ مجموعه‌تست اجرا نشد. هیچ مسیر
  رابط یا فهم صاحب پروژه به‌عنوان تأییدشده ثبت نشده است.
- قدم بعدی: بازکردن صفحهٔ خانه در staging و اعلام صفحهٔ قابل‌مشاهده؛ دستیار
  مشاهده را به فایل‌های صفحه/قاب وصل کند و اولین بخش فرانت را توضیح و بررسی کند.
- مشخصات دستگاه‌ها هنگام بررسی واکنش‌گرایی/موبایل تکمیل می‌شوند؛ شروع درس
  به دریافت آن‌ها وابسته نیست.

### checkpoint جلسهٔ ۱، مشاهدهٔ خانه — ۲۰۲۶-۰۹-۲۱

- شاهد صاحب پروژه: تصویر Chrome دسکتاپ از URL staging ثبت‌شده؛ عنوان تب
  «چین‌ورس». صفحهٔ خانه در قاب باریک وسط مرورگر دیده می‌شود. دو تب
  «فعالیت‌ها» و «روند یادگیری»، پیام «هنوز فعالیتی ثبت نشده»، پنج مسیر پایین
  «خانه/لایتنر/کاوش/ویترین/پروفایل» و دکمهٔ پشتیبانی مشاهده شدند.
- آموزش این بخش: `frontend/src/app/page.tsx` محتوای خانه و state تب‌ها را
  می‌سازد؛ `AppShell.tsx` قاب و پشتیبانی را قرار می‌دهد؛ `BottomNav.tsx`
  مسیرهای پایین را تعریف می‌کند؛ `globals.css` عرض قاب را حداکثر `430px`
  قرار داده است. بنابراین قاب باریک دسکتاپ رفتار فعلی طراحی است؛ دربارهٔ
  مطلوب‌بودن آن هنوز تصمیم ظاهری صاحب پروژه ثبت نشده است.
- داده: `frontend` برای فعالیت‌ها `/feed` را می‌خواند. backend در
  `backend/app/api/v1/endpoints/feed.py` گالری و خدمات کاربران فعال و تأییدشده
  را جمع می‌کند. GET مستقیمِ عمومی به
  `https://moamin9-chinverse-api.hf.space/api/v1/feed?limit=20` در
  `2026-09-21T12:59:55Z` پاسخ `200 []` داد. این با تصویر خالی سازگار است؛
  پاسخ Network همان مرورگر و وضعیت سایر مجموعه‌های داده دیده نشده‌اند.
- یافتهٔ کد `REV-005`: شکست درخواست `/feed` پیش‌تر همان پیام «هنوز فعالیتی
  ثبت نشده» را نشان می‌داد. فرانت محلی اکنون خطای جدا و دکمهٔ «تلاش دوباره»
  دارد؛ هنگام retry خطا پاک و حالت بارگذاری نمایش داده می‌شود.
- اصلاح: `frontend/src/app/page.tsx` و تست جدید `frontend/src/app/page.test.tsx`.
  دو تست رابط (پاسخ موفق خالی و خطا→تلاش دوباره→موفقیت) پاس شدند؛ TypeScript،
  ESLint فایل‌های مربوط و build تولیدی Next.js نیز موفق شدند.
- commit/deploy: انجام نشده؛ تغییر فقط در workspace محلی است. تصویر staging
  نسخهٔ قدیمی را نشان می‌دهد و بازآزمایی چشمی اصلاح هنوز ممکن نشده است.
- SHA frontend زنده و تأیید فهم صاحب پروژه همچنان نامشخص‌اند. مشاهدهٔ صفحهٔ
  خانه، تأیید کل فرانت یا سایر مسیرها محسوب نمی‌شود.
- اقدام بعدی صاحب پروژه: در همین خانه تب «روند یادگیری» را باز کند و چیزی که
  می‌بیند یا هر نکتهٔ ظاهری مدنظرش را بگوید. دادهٔ حسابی را در تصویر منتشر نکند.
- اقدام بعدی دستیار: مشاهدهٔ تب دوم را به کد و داده وصل کند؛ اصلاح `REV-005`
  را در بستهٔ مناسب staging مستقر و پس از آن بازآزمایی کند.

### checkpoint جلسهٔ ۱، تب «روند یادگیری» — ۲۰۲۶-۰۹-۲۱

- شاهد صاحب پروژه: تصویر تب «روند یادگیری» در staging، بدون ورود به حساب.
  کارت «آمار بار نشد» با متن بررسی اتصال و دکمهٔ «تلاش دوباره» دیده می‌شود.
- بررسی فنی: `frontend/src/components/daily/DailyPracticeContent.tsx` هنگام
  بازشدن تب `dailyActivityService.getSummary(370)` را فراخوانی می‌کند؛ سرویس
  درخواست `GET /daily-activity/summary?days=370` می‌سازد. endpoint در
  `backend/app/api/v1/endpoints/daily_activity.py` کاربر فعلی را لازم دارد
  تا آمار همان حساب را برگرداند.
- GET مستقل و بدون حساب به endpoint staging در `2026-09-21T13:12:16Z`
  پاسخ `401 {"detail":"Not authenticated"}` داد. این با وضعیت مهمان و
  تصویر سازگار است؛ درخواست Network همان مرورگر مستقلاً ثبت نشده است.
- مفهوم آموزش: `401` یعنی سرور هویت کاربر را ندارد. این با «آمار صفرِ یک
  کاربرِ واردشده» و با خطای شبکه/سرور فرق دارد. نمایش آمار شخصی به ورود وابسته است.
- یافتهٔ `REV-006`: frontend همهٔ شکست‌ها، از جمله `401`، را به پیام اتصال
  تبدیل می‌کرد. در کد محلی، `401` اکنون دعوت به ورود با بازگشت به همین تب
  را نشان می‌دهد؛ خطاهای دیگر همچنان امکان تلاش دوباره دارند.
- اصلاح: `frontend/src/components/daily/DailyPracticeContent.tsx` و تست
  `frontend/src/components/daily/DailyPracticeContent.test.tsx`.
  مجموع تست‌های مرتبط خانه/روند یادگیری `4 passed`، TypeScript، ESLint و
  build تولیدی Next.js موفق شدند.
- commit/deploy انجام نشده؛ staging هنوز رفتار قدیمی تصویر را دارد و
  تأیید چشمی اصلاح یا آمار یک حساب واردشده ثبت نشده است.
- اقدام بعدی صاحب پروژه: در نوبت بعد «کاوش» را از نوار پایین باز کند و اولین
  محتوایی را که می‌بیند شرح دهد. نیازی به ساخت حساب جدید پیش از رسیدگی به
  سیاست تأیید حساب `REV-002` نیست.
- اقدام بعدی دستیار: کاوش را به route، API و داده وصل کند و یافته‌های تصویری
  صاحب پروژه را با کد و سرویس بررسی کند. اصلاحات محلی خانه بعداً روی staging
  مستقر و در همان نسخه بازآزمایی شوند.

### checkpoint جلسهٔ ۱، «کاوش» و HSK — ۲۰۲۶-۰۹-۲۱

- شاهد صاحب پروژه: `/explore` گروه‌های موضوعی و کارت HSK را نشان می‌دهد؛
  کلیک HSK به `/explore/hsk` می‌رود و متن «هنوز محتوایی برای این بخش ثبت نشده است»
  دیده می‌شود. این مشاهدهٔ مسیر و empty-state است، نه تأیید پخش درس.
- آموزش ساختار: `frontend/src/app/explore/page.tsx` کارت‌ها را از آرایهٔ ثابت
  `exploreSections` در `frontend/src/components/explore/exploreData.ts` می‌سازد.
  بنابراین دیده‌شدن نام HSK به معنای وجود دورهٔ منتشرشده نیست. صفحهٔ HSK در
  `frontend/src/app/explore/hsk/page.tsx` از `CourseExplorePage` استفاده می‌کند؛
  آن component با `fetchCoursesBySubcategory("hsk")` فهرست عمومی را از API می‌گیرد.
- backend در `backend/app/api/v1/endpoints/courses.py` برای درخواست عمومی فقط
  دوره‌های `published` را برمی‌گرداند. دادهٔ taxonomy در DB جدا از فهرست
  دوره‌های منتشرشده است؛ draftهای احتمالی با این GETها دیده نمی‌شوند.
- شواهد زندهٔ تازه در `2026-09-21T13:21:37Z` تا `13:21:38Z`:
  `/courses/?subcategory_slug=hsk` برابر `200 []`؛ `/courses/?limit=100`
  برابر `200 []`؛ `/courses/taxonomy` برابر `200` با ۴ گروه. این درخواست‌ها
  مستقیم به backend و بدون حساب بودند؛ Network همان مرورگر ثبت نشده است.
- سابقه: گزارش مرحلهٔ ۲، دورهٔ مصنوعی با درس/رسانه را در staging آزمود و سپس
  دقیقاً آن fixture را پاک کرد. بنابراین آزمون موفق گذشته، وجود محتوای کنونی
  را ثابت نمی‌کند؛ حذف عمدیِ fixture با وضعیت کنونی سازگار است.
- `REV-001` اکنون برای مسیر اصلی «پیدا کردن و تماشای درس» یک مانع `P1` محصول
  است. این حکم دربارهٔ سفر انتخاب‌شدهٔ درس است؛ نبود همهٔ draftها یا تمام انواع
  محتوای اپ ادعا نمی‌شود.
- کار فنی این نوبت: فقط خواندن کد/گزارش و GET عمومی. تغییر برنامه، commit،
  deploy یا تست خودکار تازه‌ای انجام نشد. اصلاحات محلی `REV-005/006`
  همچنان منتظر استقرار و بازآزمایی‌اند.
- اقدام بعدی: چرخهٔ ساخت دوره، draft، انتشار و فایل رسانه را با مثال HSK
  توضیح دهیم؛ برای بازبینی واقعی پلیر، یک درس مصنوعی کوتاهِ مجاز با روش
  پشتیبانی‌شده در staging تهیه و شناسه/cleanup آن ثبت شود. نوع محتوای واقعی
  نسخهٔ اول و اینکه دستهٔ خالی به کاربر نشان داده شود، تصمیم صاحب پروژه است.

### checkpoint جلسهٔ ۱، نیاز به صفحه‌های ویدیو — ۲۰۲۶-۰۹-۲۱

- صاحب پروژه ظاهر کاوش و HSK را مناسب دانست و تأکید کرد صفحه‌های مربوط به
  ویدیو باید وجود داشته باشند. این شرط برای مسیر اصلی درس در `REV-001`
  ثبت شد؛ صرف وجود فایل صفحه، تأیید پخش روی staging نیست.
- مسیر موجود در کد: کارت منتشرشدهٔ HSK در `CourseExplorePage.tsx` به
  `/hsk/{courseId}` می‌رود؛ `frontend/src/app/hsk/[id]/page.tsx` از
  `CourseDetailPage.tsx` استفاده می‌کند. این صفحه با `GET /courses/{id}`
  اطلاعات دوره و درس‌های قابل‌نمایش را می‌گیرد و دکمهٔ «شروع» و کارت درس
  را به `/watch/hsk/{courseId}?lesson={lessonId}` وصل می‌کند.
- پلیر در `frontend/src/app/watch/[domain]/[courseId]/page.tsx` مشترک بین
  دامنه‌های محتواست؛ مسیر مستقیم `/lessons/{lessonId}` نیز در کد وجود دارد.
  playback از endpoint رسانهٔ backend لینک کوتاه‌عمر می‌گیرد؛ زیرنویس و
  وضعیت مجوز در پاسخ آن هستند. برای درس رایگانِ منتشرشده API از کاربر
  مهمان هم پشتیبانی می‌کند؛ ثبت پیشرفت روزانه به حساب کاربری وابسته است.
- در backend، نمای عمومی دوره فقط `published` را می‌بیند و درس وقتی در
  فهرست عمومی می‌آید که درس و رسانهٔ ویدیویی منتشر و مجوز رسانه تأیید شده
  باشند. پنل `/admin` تب «محتوا و ویدیو» برای ساخت/انتشار دارد؛
  `/admin/content` در کد فعلی به `/admin` هدایت می‌شود.
- چرخهٔ نمونهٔ قابل‌دیدن: تصویر جلد و ویدیوی ساخته‌شده برای آزمون در storage
  خصوصی staging قرار می‌گیرند؛ هر asset با checksum ثبت، مجوزش بررسی و منتشر
  می‌شود؛ سپس دوره/بخش/درس رایگان ساخته و درس و دوره منتشر می‌شوند. زیرنویس
  منتشرشده را نیز برای بازبینی پلیر اضافه می‌کنیم. seed قدیمی `seed_lms.py`
  این چرخه را انجام نمی‌دهد و برای staging مناسب نیست.
- برای اجرای بخش مدیریتی روی staging نشست معتبر مدیر با MFA لازم است؛ در
  محیط فعلی چنین نشستی در اختیار دستیار نیست. صاحب پروژه می‌تواند خودش با
  حساب موجود وارد شود و پنل `/admin` را باز کند؛ هیچ رمز یا کد MFA نباید در
  گفتگو فرستاده شود. تا آن زمان وجود routeها در کد ثابت است، اما نمایش و
  پخش واقعی هنوز تأیید نشده است.
- شواهد فنی: کد مسیرها و تست موجود `phase4-launch-mobile.spec.ts` با دادهٔ
  مصنوعی خوانده شدند؛ build قبلی Next.js routeهای `/hsk/[id]`،
  `/watch/[domain]/[courseId]` و `/lessons/[id]` را تولید کرده بود. یک اجرای
  تازهٔ Playwright با project نامناسب `desktop-chromium` در راه‌اندازی
  متوقف و قطع شد؛ **هیچ pass تازه‌ای از آن ثبت نمی‌شود**. تغییر کد، deploy
  یا آزمون ویدیوی زنده در این بخش انجام نشد.
- اقدام بعدی دستیار: برای staging یک محتوای مصنوعی کوتاه و رایگان از workflow
  معتبر محتوا آماده کند، شناسه‌ها و روش cleanup را ثبت کند و سپس مسیر
  «HSK → دوره → درس → پخش/زیرنویس» را روی همان نسخه مرور کند. دادهٔ واقعی
  نسخهٔ اول از این نمونه جداست و انتخاب محتوای واقعی با صاحب پروژه است.
- اقدام بعدی صاحب پروژه پس از آماده‌شدن نمونه: ظاهر صفحهٔ دوره و پلیر را
  در مرورگر خود ببیند و دربارهٔ چینش/کنترل‌ها ویدیو بازخورد بدهد.

### checkpoint جلسهٔ ۱، بالای پنل ادمین — ۲۰۲۶-۰۹-۲۱

- صاحب پروژه با سه تصویر نشان داد که پنل `/admin` باز است؛ فشردگی عنوان و
  توضیح در بالای پنل و بریده‌شدن تب‌ها را به‌درستی گزارش کرد. تصویر
  دیکشنری پیام بارگذاری ۳۰۰ کلمه را نشان می‌دهد، نه محتوای دوره‌ها.
- مفهوم این بخش: `.app-frame` در `globals.css` حداکثر ۴۳۰ پیکسل است؛
  چون دسکتاپ هم همین قاب را نمایش می‌دهد، breakpoint مبتنی بر عرض پنجره
  برای این سربرگ مناسب نیست. خود پنل باید در عرض قاب جا شود.
- اصلاح محلی در `frontend/src/app/admin/page.tsx`: سربرگ دو ردیفه و تب‌ها
  چندردیفه شدند تا عنوان و همهٔ گزینه‌ها خوانا بمانند. typecheck، lint فایل
  و build موفق‌اند؛ Playwright در این محیط نتیجهٔ نهایی نداد. `REV-007`
  تا deploy و بازبینی چشمی باز است.
- قدم بعدی: نسخهٔ اصلاح‌شده را به staging رسانده و بالای پنل را در همان
  عرض بازبینی کنیم؛ سپس به ساخت درس ویدیویی نمونه برگردیم.

#### نتیجهٔ نهایی این checkpoint

- commit `8e8ccd4` منتشر شد؛ Quality gate، استقرار backend و smoke exact-SHA
  موفق‌اند و backend همان SHA را گزارش می‌کند. status Vercel نیز موفق بود.
- صاحب پروژه پس از تازه‌سازی پنل گفت عنوان و هر پنج تب درست شده‌اند؛
  `REV-007` بسته شد. اصلاحات خانه `REV-005/006` در همین نسخه مستقر شدند،
  ولی حالت‌های مربوطشان هنوز بازآزمایی چشمی نشده‌اند.
- بخش آموزشی بعدی: تفاوت رکورد دوره/درس در DB با فایل ویدیو در storage و
  ترتیب انتشار آن‌ها در پنل؛ سپس نمونهٔ مجاز برای بازدید پلیر.

### checkpoint جلسهٔ ۱، داده و فایل درس نمونه — ۲۰۲۶-۰۹-۲۱

- نقشهٔ همین مسیر: اطلاعات دوره، بخش، درس، شناسهٔ رسانه و وضعیت انتشار در
  PostgreSQL شاخهٔ staging در Neon نگه‌داری می‌شود؛ بایت‌های MP4/تصویر در
  storage متصل به backend روی Hugging Face (`FILE_STORAGE_MODE=mounted`،
  mount تاریخی `/data`) قرار می‌گیرند. فرانت‌اند روی Vercel از API backend
  می‌خواند. پلیر پس از بررسی مجوز/دسترسی از backend لینک امضاشدهٔ کوتاه‌عمر
  دریافت می‌کند. سالم‌بودن دیتابیس و storage به معنی وجود دوره نیست.
- پنل فعلی فایل را مستقیماً upload نمی‌کند؛ فرم «ساخت دوره» شناسهٔ asset جلد
  و فرم «افزودن درس» شناسهٔ asset ویدیو را می‌خواهد. فرم «ثبت و ممیزی رسانه»
  نیز مسیر/کلید فایل موجود در storage، SHA-256، منبع و مجوز را می‌گیرد.
  endpoint قدیمی upload درس وجود دارد، اما به‌تنهایی اطلاعات مجوز و checksum
  لازم برای انتشار فعلی را تکمیل نمی‌کند؛ برای نمونهٔ staging از آن به‌عنوان
  میان‌بُر انتشار استفاده نشود.
- در پوشهٔ نادیده‌گرفته‌شدهٔ
  `E:/Chinverse/.tmp/owner-review-fixture/` ویدیوی مصنوعی ۴ ثانیه‌ای
  H.264/AAC، جلد PNG و زیرنویس SRT فارسی ساخته شد. `manifest.json` اندازه
  و checksum دقیقشان را دارد؛ MP4 کامل decode شد. این‌ها محتوای آموزشی واقعی
  نسخهٔ اول نیستند و هنوز به staging فرستاده نشده‌اند.
- GET تازهٔ `/health/ready`: `status=ok` و `database_target/database/storage=ok`؛
  فهرست عمومی HSK همچنان صفر دوره دارد. `REV-001` باز است.
- قدم کوچک بعد: شیوهٔ دسترسی به storage خصوصی staging را در نشست مجاز مشخص
  کنیم، همین دو فایل رسانه را آنجا قرار دهیم و سپس در پنل با شناسه‌ها و
  cleanup ثبت‌شده، درس رایگان را منتشر کنیم؛ پس از آن بازبینی صفحهٔ دوره/پلیر.

### checkpoint جلسهٔ ۱، مسیر آپلود رسانه از پنل — ۲۰۲۶-۰۹-۲۱

- علت کار: صفحه‌های دوره و پلیر در کد هستند، اما بدون دوره/درس منتشرشده در
  کاتالوگ عمومی دیده نمی‌شوند. پنل فقط مسیر فایل از پیش ذخیره‌شده را می‌گرفت؛
  برای نمونهٔ مجاز، صاحب پروژه به راهی برای ثبت خود فایل نیاز داشت.
- به endpoint محافظت‌شدهٔ ادمین و تب «محتوا و ویدیو»، آپلود مستقیم تصویر/ویدیو
  افزوده شد. سرور فایل را با اعتبارسنجی نوع و حجم در storage خصوصی ذخیره و
  SHA-256 **بایت ذخیره‌شده** را ثبت می‌کند. رکورد ابتدا draft و مجوز pending
  است؛ آپلود به‌تنهایی چیزی را عمومی نمی‌کند. اگر commit دیتابیس شکست بخورد،
  فایل تازه حذف می‌شود. پس از انتشار، تصویر به فیلد جلد دوره و ویدیو به فیلد
  رسانهٔ درس منتقل می‌شود.
- مفهوم: «فایل» بایت‌های MP4/تصویر در storage است؛ «asset» رکوردی در Neon
  شامل مسیر خصوصی، checksum، منبع، حقوق و وضعیت انتشار است. `media_id` در
  درس به این رکورد اشاره می‌کند، نه به URL خام فایل. زیرنویس نیز مرحلهٔ
  جداگانهٔ ثبت/اعتبارسنجی/انتشار دارد.
- تست محلی: ۲۸ تست backend مربوط به رسانه/انتشار، ۶ تست سرویس پنل، typecheck،
  lint بدون error و build فرانت موفق شدند. تست خصوصیِ گسترده‌تر به علت نبود
  `prometheus_client` در محیط پایتون این دستگاه پیش از اجرا متوقف شد؛ pass
  محسوب نمی‌شود. بازبینی تصویری پنل تازه هنوز انجام نشده است.
- فایل‌های مصنوعی `.tmp/owner-review-fixture` هنوز محلی‌اند. تا ثبت شناسهٔ
  رسانه/دوره/درس در staging و بازدید واقعی پلیر، `REV-001` باز می‌ماند.
- قدم بعد: تأیید استقرار همین تغییر؛ سپس در پنل یک جلد و ویدیوی مصنوعی را
  بارگذاری و پس از ممیزی مجوز منتشر کنیم. دوره/درس رایگانِ نمونه و زیرنویس
  با شناسه و برنامهٔ پاک‌سازی ثبت می‌شوند؛ صاحب پروژه صفحهٔ دوره و پلیر را می‌بیند.

## متن آماده برای گفت‌وگوی تازه

```text
می‌خواهم کل پروژهٔ Chinverse و کارهای انجام‌شده را باحوصله یاد بگیرم و هم‌زمان
بخش‌به‌بخش، به‌ویژه فرانت‌اند، آن را ببینم و اصلاح کنم تا به لانچ برسیم.
در پروژهٔ E:/Chinverse ابتدا docs/OWNER_LAUNCH_WALKTHROUGH_FA.md و
docs/OWNER_LAUNCH_REVIEW_LOG_FA.md را بخوان. از نقطهٔ ادامهٔ ثبت‌شده شروع کن.
آموزش را به چک‌لیست لانچ یا سه مسیر کاربر محدود نکن. ابتدا تصویر کلی، سپس
صفحهٔ واقعی، کد و متغیرها، داده و سرویس‌های مرتبط را با دلیل پیاده‌سازی توضیح بده.
هر نوبت یک موضوع کوچک و حداکثر ۳ تا ۵ اقدام ساده داشته باشد.
کارهای چشمی ساده را من انجام می‌دهم؛ بررسی کد، اصلاح و تست را خودت پیش ببر.
بعد از هر بخش، نتیجهٔ تست، ایرادها و قدم بعدی را در همین فایل‌ها به‌روز کن.
تکمیل مرحلهٔ ۷ فقط CLOSED-PREPUBLIC بوده؛ برای لانچ عمومی دوباره ارزیابی لازم است.
از نقطهٔ ادامهٔ تازه شروع کن: خانه، کاوش/HSK و پنل ادمین دیده شده‌اند.
مسیر صفحهٔ دوره/پلیر در کد وجود دارد، ولی روی staging به دلیل نبود دورهٔ
منتشرشده دیده نشده است. سربرگ و تب‌های پنل ادمین در `REV-007` روی staging
اصلاح و تأیید شده‌اند. فایل‌های مصنوعی نمونه در `.tmp/owner-review-fixture`
آماده‌اند؛ بخش بعد دسترسی به storage staging و ثبت/انتشار همین fixture است.
`REV-001` برای مسیر درس باز است؛ `REV-005/006` مستقر شده‌اند ولی بازآزمایی
چشمیِ حالت‌هایشان باقی است. سه مسیر قبلاً تأیید شده‌اند؛
دوباره آن‌ها را سؤال نکن.
```

## منابع ادامه

- [برنامهٔ قبلی و تاریخچهٔ مرحله‌ها](LAUNCH_READINESS_ACTION_PLAN_FA.md)
- [گزارش بسته‌شدن مرحلهٔ ۷](LAUNCH_READINESS_STAGE_7_REPORT_FA.md)
- [گزارش دستگاه‌های واقعی](PHASE_4_MOBILE_ACCESSIBILITY_REPORT_FA.md)
- [ثبت معافیت‌ها و blockerها](PHASE_8_RELEASE_BLOCKERS.json)
- [runbook انتشار](PHASE_8_RELEASE_RUNBOOK_FA.md)
