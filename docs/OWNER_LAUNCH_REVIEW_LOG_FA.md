# دفتر بازبینی دستی و یادگیری پیش از لانچ

آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۲۳؛ مسیر جلسات در [راهنمای همراهی](OWNER_LAUNCH_WALKTHROUGH_FA.md).

این فهرست issue tracker زنده یا ادعای ممیزی کامل نیست. موارد تأییدشدهٔ مسدودکننده
پیش از تصمیم انتشار باید با inventory انتشار و issue tracker همگام شوند.

**مسیر فعال طبق توضیح تکمیلی صاحب پروژه:** یادگیری جامع پروژه همراه با مشاهده
و اصلاح. خانه، پنل محتوا و مسیر ویدیو مشاهده شده‌اند؛ HSK اکنون ۹ سطح و
صفحهٔ آمادهٔ درس دارد و تأیید چشمی همین طراحی توسط صاحب پروژه باقی است. سه
اولویت محصول قبلی محدودهٔ آموزش نیستند. پرسش‌های دستگاه‌ها باقی‌اند، اما مانع شروع آموزش نیستند.
فهرست پوشش مفاهیم و آخرین checkpoint در راهنمای همراهی به‌روز شده است.

## ثبت سریع توسط صاحب پروژه

برای هر مورد این مقدار کافی است؛ دستیار جزئیات فنی را تکمیل می‌کند:

```text
صفحه / دستگاه:
کاری که کردم:
انتظارم:
چیزی که دیدم:
عکس یا ویدیوی کوتاه، اگر لازم بود (بدون رمز/کد/اطلاعات شخصی):
```

نوع مورد: خرابی، ابهام در استفاده، پیشنهاد ظاهری، کمبود محتوا، یا خلأ شواهد.
اولویت پس از بازتولید تعیین می‌شود: P0 اختلال/خطر بحرانی؛ P1 مانع مسیر اصلی؛
P2 مشکل مهم با راه‌حل موقت؛ P3 بهبود جزئی. «باز»، «آمادهٔ بازآزمایی»، «تأییدشده»
و «موکول‌شده با دلیل» وضعیت‌های متفاوت‌اند. «اصلاح کد شد» به معنی تأیید چشمی نیست.

## فهرست آغازین

| شناسه | موضوع | منشأ شاهد | وضعیت / اقدام بعدی |
|---|---|---|---|
| REV-001 | کاتالوگ عمومی staging خالی بود | تصویر HSK + کاتالوگ ۹ سطح و fixture زنده | `P1`؛ ساختار کامل و آزمون فنی staging موفق؛ آمادهٔ تأیید چشمی صاحب پروژه |
| REV-002 | تأیید حساب و بازیابی باید با نبود SMS/email واقعی هماهنگ شوند | کد و تصمیم صاحب پروژه | تعیین سیاست و بازتولید با کاربر جدید؛ مانع بالقوهٔ مسیر اصلی |
| REV-003 | نتیجهٔ دستگاه واقعی کامل ثبت نشده | گزارش مرحلهٔ ۴ | باز در جلسهٔ دستگاه؛ باگ اثبات‌شده نیست |
| REV-004 | verifier انتشار دامنهٔ waiver غیرعمومی را اعمال نمی‌کند | بررسی مستقیم script/workflow | اصلاح لازم قبل از rollout عمومی؛ وضعیت آن را صرفاً با CI سبز نبند |
| REV-005 | خطای دریافت فید در خانه به‌اشتباه حالت «هنوز فعالیتی ثبت نشده» را نشان می‌داد | از کد و تست رابط | اصلاح در staging `8e8ccd4`؛ بازآزمایی حالت خطا باقی است |
| REV-006 | تب روند یادگیری، پاسخ `401` مهمان را خطای اتصال نشان می‌داد | تصویر صاحب پروژه + کد + GET بی‌حساب staging | اصلاح در staging `8e8ccd4`؛ بازآزمایی چشمی باقی است |
| REV-007 | سربرگ و تب‌های پنل ادمین در قاب باریک فشرده/بریده می‌شوند | سه تصویر صاحب پروژه + کد + تأیید پس از refresh | `P2`؛ اصلاح و تأیید چشمی روی staging؛ بسته |

### REV-001 — محتوای قابل‌تست

- تاریخ/محیط: ۲۰۲۶-۰۹-۲۰، staging.
- درخواست: `GET https://moamin9-chinverse-api.hf.space/api/v1/courses/?limit=1`.
- مشاهده: `200 []`؛ readiness دیتابیس/storage هم‌زمان `ok` بود.
- تفسیر: فهرست عمومی این درخواست خالی است؛ وضعیت draftهای ادمین بررسی نشده است.
- اثر: empty-state قابل بررسی است، اما مسیر انتخاب درس تا پخش با این فهرست
  قابل تأیید نیست. نبود محتوای اصلی در نسخهٔ لانچ ممکن است P1 محصول باشد.
- اقدام: پنل محتوا را بررسی و یک درس نمونهٔ مصنوعی/رایگان از workflow پشتیبانی‌شده
  آماده کن؛ محتوای واقعی نسخهٔ اول را صاحب پروژه انتخاب کند.
- شرط بستن: با حساب عادی درس دیده و پخش شود؛ subtitle، پیشرفت، refresh و موبایل
  بررسی شوند. شناسهٔ fixture و نگه‌داری/cleanup آن ثبت شود.
- اصلاح/بازآزمایی: هنوز انجام نشده.

#### شواهد تکمیلی ۲۰۲۶-۰۹-۲۱

- صاحب پروژه در Chrome مسیر `/explore`→`/explore/hsk` را پیمود. گروه‌های
  کاوش دیده شدند ولی HSK پیام نبود محتوا داشت.
- GET عمومی backend در `13:21:37Z` تا `13:21:38Z`: HSK با
  `subcategory_slug=hsk` برابر `200 []`؛ کل فهرست منتشرشده با `limit=100`
  برابر `200 []`؛ taxonomy برابر `200` با ۴ گروه. این‌ها شواهد مستقیم
  فهرست عمومی‌اند، نه مشاهدهٔ تمام رکوردهای دیتابیس.
- علت قابل توضیح از کد: صفحهٔ اول کاوش از فهرست ثابت frontend ساخته می‌شود؛
  صفحهٔ HSK از API دوره‌های `published` می‌خواند. پس نمایش کارت و خالی‌بودن
  فهرست می‌توانند هم‌زمان درست باشند. draftها از درخواست عمومی دیده نمی‌شوند.
- گزارش مرحلهٔ ۲ ثبت کرده که fixture دوره/درس/رسانه پس از آزمون موفق عمداً
  از Neon staging و storage پاک شده است؛ وجود آن در گذشته دلیل وجود امروز نیست.
- اولویت اکنون `P1` برای سفر تأییدشدهٔ «پیدا کردن و تماشای درس» است؛
  سایر قابلیت‌ها با این شاهد قضاوت نمی‌شوند. تا محتوای نمونهٔ منتشرشده و
  بازبینی پخش با حساب عادی، این سفر pass نمی‌شود.
- اقدام فنی بعدی: بررسی workflow واقعی پنل `/admin` و ساخت یک نمونهٔ
  مصنوعی کوتاه با ثبت id و برنامهٔ cleanup؛ قبل از ساخت محتوای واقعی، انتخاب
  موضوع/محتوای نسخهٔ اول با صاحب پروژه است.

#### تأکید صاحب پروژه دربارهٔ ویدیو — ۲۰۲۶-۰۹-۲۱

- ظاهر فعلی کاوش/HSK از نظر صاحب پروژه قابل‌قبول است. خواستهٔ صریح او:
  **صفحه‌های مربوط به ویدیو باید وجود داشته و قابل‌بررسی باشند**.
- کد فعلی مسیر HSK→`/hsk/{courseId}` (صفحهٔ دوره و درس‌ها)→
  `/watch/hsk/{courseId}?lesson={lessonId}` (پلیر مشترک) را دارد؛
  `/lessons/{lessonId}` نیز صفحهٔ مستقیم پخش است. لینک‌های مسیر در
  `CourseExplorePage.tsx` و `CourseDetailPage.tsx` بررسی شدند.
- درس عمومی فقط وقتی در پاسخ دوره می‌آید که درس و رسانه منتشر و مجوز رسانه
  تأیید شده باشد. درس رایگانِ منتشرشده برای پخش می‌تواند بدون ورود باز شود؛
  تجربهٔ واقعی روی staging اکنون به علت نبود محتوا آزموده نشده است.
- build پیشین routeهای صفحهٔ دوره و پلیر را تولید کرده بود. اجرای تازهٔ
  Playwright برای این بررسی به علت استفاده از project نامناسب/راه‌اندازی
  طولانی قطع شد؛ نتیجهٔ pass از آن ادعا نمی‌شود. تست ویدیو با mock که در
  repository وجود دارد، جایگزین بازبینی staging با رسانهٔ واقعی نیست.
- شرط تکمیل `REV-001`: یک درس مصنوعی کوتاه و رایگان از workflow معتبر در
  staging منتشر شود؛ صفحهٔ دوره، فهرست درس، کنترل‌های پلیر، زیرنویس و
  بازگشت/refresh توسط صاحب پروژه دیده شوند؛ شناسه و cleanup ثبت شود.
- بررسی workflow: `course_admin.py` برای ایجاد دوره asset جلد ثبت‌شده و برای
  درس asset ویدیویی ثبت‌شده می‌خواهد. پنل `/admin` عملیات ثبت/بررسی مجوز/
  انتشار رسانه، ساخت درس و انتشار درس/دوره را دارد. `seed_lms.py` قدیمی
  MediaAsset و publication state نمی‌سازد و نباید برای staging اجرا شود.
  ساخت fixture زنده به نشست admin معتبر با MFA و storage خصوصی staging نیاز
  دارد؛ در این نوبت چنین دسترسی‌ای فراهم نبود و هیچ fixture ساخته نشد.

#### آماده‌سازی محلی نمونهٔ مجاز — ۲۰۲۶-۰۹-۲۱

- فقط در `E:/Chinverse/.tmp/owner-review-fixture/` (gitignored) سه فایل
  کاملاً مصنوعی ساخته شد: جلد PNG، ویدیوی ۴ ثانیه‌ای H.264/AAC با تصویر و
  صدای آزمایشی، و زیرنویس فارسی SRT. `manifest.json` اندازه و SHA-256 هر
  فایل را ثبت می‌کند؛ checksumها دوباره تطبیق داده شدند و MP4 با ffmpeg
  کامل decode شد. فایل‌های ویدیویی قبلی مخزن به دلیل نامعلوم‌بودن حقوقشان
  استفاده نشدند.
- کد پنل فقط asset از قبل ذخیره‌شده را ثبت می‌کند و کنترل upload همین فایل‌ها
  ندارد. endpoint قدیمی `lessons/upload` رسانه را ایجاد می‌کند، ولی
  checksum و provenance موردنیاز انتشار جدید را تکمیل نمی‌کند؛ میان‌بُر
  مطمئنی برای بستن `REV-001` نیست.
- GET زندهٔ staging: `/health/ready` برابر `status=ok` و سه check
  `database_target/database/storage=ok`؛ فهرست عمومی HSK هنوز صفر مورد است.
  هیچ فایل یا رکوردی روی staging ساخته/منتشر نشده و صفحهٔ پلیر هنوز دیده نشده.
- مرحلهٔ بعد: access مجاز storage خصوصی staging، upload دقیق همین fixture،
  ثبت asset/مجوز/شناسه و انتشار درس/دوره؛ سپس تأیید پخش و cleanup plan.

#### مسیر آپلود مستقیم برای نمونه — ۲۰۲۶-۰۹-۲۱

- مسیر فنی تازه در پنل ادمین و backend پیاده‌سازی شد: upload تصویر/ویدیو با
  دسترسی admin، ذخیرهٔ خصوصی، محاسبهٔ checksum واقعی، ثبت draft/pending و
  حذف فایل در صورت شکست commit. ثبت منبع/دارندهٔ حقوق/نوع مجوز همچنان الزامی
  است؛ بررسی مجوز و انتشار دو اقدام مستقل‌اند.
- تست فنی محلی: ۲۸ تست backend رسانه/انتشار و ۶ تست سرویس frontend موفق؛
  typecheck، lint بدون error و build موفق. مجموعهٔ `test_phase5_private_media`
  در محیط محلی به دلیل نبود `prometheus_client` جمع‌آوری نشد. رفتار staging
  و ظاهر پنل جدید هنوز مشاهده نشده‌اند.
- اجرای اولیهٔ CI در lint قانون `react-hooks/set-state-in-effect` را روی پاک‌کردن
  فایل انتخاب‌شده پیدا کرد. منطق به handler تغییر نوع رسانه منتقل و lint با
  `CI=true`، typecheck و تست سرویس دوباره موفق شد؛ SHA اولیه مبنای تأیید نیست.
- بازبینی تصویری نسخهٔ تازه در قاب باریک، متن انگلیسی و فشردهٔ کنترل native
  فایل را نشان داد. کنترل به برچسب فارسی «انتخاب فایل / فایلی انتخاب نشده» و
  نمایش کوتاه‌شدهٔ نام فایل تبدیل شد؛ بازآزمایی staging پس از استقرار لازم است.
- **وضعیت `REV-001`: باز.** وجود کنترل آپلود به‌معنی وجود درس منتشرشده نیست.
  fixture هنوز فقط در `.tmp/owner-review-fixture` است؛ شناسهٔ asset/دوره/درس
  و برنامهٔ cleanup پس از ساخت واقعی در staging به این گزارش افزوده می‌شوند.

#### fixture منتشرشده و آزمون پلیر — ۲۰۲۶-۰۹-۲۲

- runtime نهایی: `e3972a2317c9a4e7d9ddaff421795560147a8407`؛ هر دو Quality
  gate، backend deploy و exact-SHA smoke موفق. کنترل فایل فارسی روی staging
  دیده شد.
- inventory fixture: cover media `3`، video media `4`، course `70`، section
  `70`، lesson `206`، subtitle track `2`؛ slug دوره
  `owner-review-video-20260921`. وضعیت course/lesson/media/subtitle منتشر و
  license هر دو asset تأیید شده است. fixture رایگان و مصنوعی است.
- مشاهدهٔ فنی مرورگر: کارت در `/explore/hsk`، صفحهٔ دوره در `/hsk/70` و پلیر
  در `/watch/hsk/70?lesson=206` دیده شدند. MP4 چهار ثانیه‌ای بدون media error
  پخش شد؛ cue دوم seek را به `2.12s` برد؛ refresh یک signed URL تازه داد و
  زیرنویس/مدت ۰۰:۰۴ حفظ شد.
- probe بدون احراز هویت: `public_hsk_count=1`، entitlement=`free_lesson`،
  subtitle count=`1` و دریافت Range برابر `206`، `video/mp4` و
  `bytes 0-63/251613`. این شاهد پخش مهمان است، نه آزمون دستگاه واقعی.
- نکتهٔ داده: پنل قبل از fixture، ۶۸ course و ۲۰۴ lesson نشان می‌داد ولی API
  عمومی صفر دوره داشت. DB خالی نبود؛ زنجیرهٔ انتشار معتبر وجود نداشت.
- cleanup پس از پایان بازبینی صاحب پروژه: آرشیو course `70` و media `3/4` و
  پاک‌سازی ثبت‌شدهٔ section `70`، lesson `206` و subtitle `2`. تا آن زمان
  fixture فقط در staging محافظت‌شده و non-indexable بماند.
- **وضعیت `REV-001`: آمادهٔ بازآزمایی چشمی صاحب پروژه.** آزمون فنی موفق است؛
  بسته‌شدن به مشاهدهٔ کارت، صفحهٔ دوره، پخش، زیرنویس، refresh و بازگشت در
  مرورگر صاحب پروژه وابسته است.

### REV-002 — کاربر جدید بدون delivery واقعی

- شاهد: `frontend/src/app/verify-account/page.tsx` تأیید ایمیل و شماره موبایل
  را نمایش می‌دهد. در `backend/app/api/v1/endpoints/auth.py` تأیید هر کانال،
  `is_verified` را وابسته به تأیید کانال دیگر می‌کند؛ guardهای login و برخی
  مسیرهای اجتماعی از verified بودن استفاده می‌کنند.
- دامنهٔ فعلی: صاحب پروژه ارسال واقعی SMS/email ندارد؛ تنظیمات live مربوط به
  الزام تأیید حساب در این نوبت خوانده نشده و signup جدید اجرا نشده است.
- اثر احتمالی: کاربر جدید می‌تواند در مرحلهٔ تأیید متوقف شود؛ موفقیت حساب ادمین
  قدیمی این مسیر را اثبات نمی‌کند.
- اقدام: با صاحب پروژه سیاست ورود و بازیابی نسخهٔ اول را تعیین کن؛ سپس راه‌حل
  انتخاب‌شده را در رابط، backend و پیام‌های واقعی هماهنگ و با حساب جدید تست کن.
- شرط بستن: ثبت‌نام، ورود و بازیابی بدون وعدهٔ ارسال غیرواقعی یا تأیید صوری
  طی شوند؛ admin/MFA و مجوزهای کاربران همچنان درست کار کنند.
- اصلاح/بازآزمایی: هنوز انجام نشده؛ اولویت نهایی پس از بازتولید.

### REV-003 — آزمون دستگاه واقعی

- شاهد: `PHASE_4_MOBILE_ACCESSIBILITY_REPORT_FA.md` موارد Android Chrome،
  iOS Safari، VoiceOver/TalkBack و PWA واقعی را کامل‌شده اعلام نمی‌کند.
- اقدام: دستگاه‌های در دسترس را ثبت و مسیرهای داخل دامنهٔ نسخهٔ اول را اجرا کن.
- شرط بستن: مدل/OS/browser، release، نتیجه و شاهد هر مسیر ثبت شوند؛ مورد
  تست‌نشده به‌عنوان pass یا معادل شبیه‌سازی گزارش نشود.
- اصلاح/بازآزمایی: منتظر آزمون صاحب پروژه؛ فعلاً خلأ شواهد است.

### REV-004 — waiver غیرعمومی در gate عمومی

- شاهد: `scripts/verify-phase8-release.ps1` با `-RequireAllProviderGates`
  فقط pending بودن را رد می‌کند؛ waiver دارای توضیح و صاحب تصمیم پذیرفته می‌شود.
- `.github/workflows/phase8-release-gate.yml` نیز در بررسی providerها همین
  پذیرش را دارد؛ `release_scope.public_launch=false` یا `CLOSED-PREPUBLIC`
  در این بررسی‌ها برای جلوگیری از استفادهٔ waiver در public ارزیابی نمی‌شود.
- اثر: سبزشدن این بررسی، اعتبار production معافیت‌های `SKIPPED-PREPUBLIC`
  را ثابت نمی‌کند. سایر شروط workflow و approvalها وجود دارند؛ این یافته
  به معنی اجرای خودکار production یا دورزدن همهٔ محافظ‌ها نیست.
- اقدام قبل از انتشار: دامنهٔ انتشار را در verifier/workflow اعمال کن؛ موارد
  صرفاً غیرعمومی نباید برای public کافی باشند. waiver پرداخت رایگان باید فقط
  وقتی محصول واقعاً رایگان و پرداخت خاموش است معتبر بماند.
- شرط بستن: تست منفی ثابت کند public با waiver غیرعمومی رد می‌شود و بررسی
  staging همچنان مجاز است. هنگام تصمیم public، inventory انتشار نیز همگام شود.
- اصلاح/بازآزمایی: هنوز انجام نشده؛ در برنامهٔ آماده‌سازی انتشار پیگیری شود.

### REV-005 — خطای فید و حالت خالی خانه

- منشأ: **از کد پیدا شد**، نه خرابی مشاهده‌شده در تصویر صاحب پروژه.
- رفتار پیشین: `loadFeed` در `frontend/src/app/page.tsx` هنگام خطای `/feed`
  فقط در console ثبت می‌کرد؛ `feedItems` خالی می‌ماند و پس از پایان loading
  همان متن «هنوز فعالیتی ثبت نشده» ظاهر می‌شد.
- اثر: کاربر خطای موقت شبکه/سرور را با نبود محتوا اشتباه می‌گرفت؛ اولویت
  پیشنهادی `P2`، تأیید نهایی پس از بازآزمایی. این یافته ثابت نمی‌کند درخواست
  مرورگر صاحب پروژه در تصویر خطا خورده است.
- اصلاح محلی: state مستقل `feedError`، پیام خطای مشخص و دکمهٔ تلاش دوباره.
  `frontend/src/app/page.test.tsx` خالی‌بودن پاسخ موفق و بازیابی بعد از خطا
  را جداگانه بررسی می‌کند.
- بررسی: `2 passed` در Vitest برای تست خانه؛ `npm run typecheck`، ESLint
  هر دو فایل تغییرکرده و `npm run build` همگی موفق.
- وضعیت: **آمادهٔ deploy و بازآزمایی**؛ هنوز commit/deploy نشده و صاحب پروژه
  اصلاح را در staging ندیده است. بستن مورد به مشاهدهٔ نسخهٔ مستقر وابسته است.

### REV-006 — پیام اشتباه برای مهمان در آمار روزانه

- منشأ: **در تصویر صاحب پروژه مشاهده شد**؛ او در هیچ حساب Chinverse وارد
  نشده بود. متن «آمار بار نشد / اتصال را بررسی کن / تلاش دوباره» دیده شد.
- تأیید علت: endpoint آمار به `get_current_user` وابسته است. GET مستقیم و
  بی‌حساب به `/api/v1/daily-activity/summary?days=370` در
  `2026-09-21T13:12:16Z` پاسخ `401 Not authenticated` داد. درخواست همان
  مرورگر ثبت نشده، ولی تصویر و پاسخ مستقل با این مسیر سازگارند.
- رفتار درست: مهمان آمار شخصی ندارد؛ صفحه باید نیاز به ورود را توضیح دهد.
  کاربر واردشده با فعالیت صفر، حالت جداگانه‌ای است و هنوز چشمی آزموده نشده.
- علت کد: `DailyPracticeContent.tsx` همهٔ خطاهای `getSummary` را یکسان به
  خطای اتصال تبدیل می‌کرد. اولویت پیشنهادی `P2`: مانع آمار شخصی است اما مسیر
  ورود در اپ وجود دارد؛ در بازآزمایی نهایی تعیین شود.
- اصلاح محلی: پاسخ `401` به کارت «برای دیدن روند یادگیری وارد حساب شو»
  و لینک ورود با بازگشت به `/?tab=daily` تبدیل شد. خطاهای دیگر همچنان
  با پیام خطا و تلاش دوباره نمایش داده می‌شوند.
- بررسی: تست مهمان (`401`) و خطای `503`→تلاش دوباره→تغییر حالت پاس شدند؛
  مجموع تست‌های مرتبط این دو بخش `4 passed`، TypeScript، ESLint و build
  تولیدی Next.js موفق.
- وضعیت: **آمادهٔ deploy و بازآزمایی**؛ هنوز commit/deploy نشده است.
  نمایش درست مهمان و آمار حساب واردشده روی staging پس از استقرار بررسی شود.

## پیشرفت جلسهٔ ۱، نوبت اول — ۲۰۲۶-۰۹-۲۰

### تصمیم محصول و دستگاه‌ها

- سه کار اصلی با صاحب پروژه تأیید شد: پیدا کردن و تماشای درس چینی؛ ذخیره و
  مرور واژه‌ها در لایتنر؛ ساخت پروفایل و ارتباط با دیگران.
- صاحب پروژه صریحاً خواست ثبت‌نام و ورود لحاظ شوند. مسیر حساب، شامل بررسی
  تأیید حساب، خروج و بازیابی، پیش‌نیاز مشترک بازبینی این سه کار است.
- این تأیید، تصمیمی برای حذف تأیید حساب یا انتخاب کانال SMS/email نیست؛
  سیاست مربوط به `REV-002` هنوز باز است.
- دستگاه شروع، مرورگر و دسترسی به Android/iPhone پرسیده شده‌اند؛ هنوز پاسخی
  دریافت نشده است. دستگاه نامشخص به‌معنی نبود دستگاه یا آزمون موفق نیست.

### شواهد تازهٔ فنی؛ مستقل از تأیید چشمی

زمان GETها: `2026-09-20T10:02:33Z` تا `10:02:35Z`، حدود ۱۳:۳۲ به وقت ایران.

| بررسی دستیار | نتیجهٔ مشاهده‌شده | تأیید چشمی صاحب پروژه / اثر |
|---|---|---|
| backend `/health` | `200`، staging، `indexable=false`؛ release=`54973b82341924f5056ba1d8d3cbb108c5c166ab` | سلامت مسیر کاربر را به‌تنهایی اثبات نمی‌کند |
| backend `/health/ready` | `200`؛ database_target، database و storage همگی `ok` | پخش واقعی ویدیو هنوز آزموده نشده است |
| کاتالوگ عمومی `?limit=1` | `200 []` | `REV-001` باز؛ وضعیت draftها نامشخص |
| frontend `/` و `/api/health` بدون نشست | هر دو `302` به `vercel.com` | دسترسی مجاز، SHA frontend و مشاهدهٔ صفحه منتظر بررسی‌اند |
| دستگاه واقعی | هنوز اطلاعات دستگاه دریافت نشده است | `REV-003` باز؛ هیچ pass ثبت نشده است |

- نشانی frontend: `https://chinverse-git-codex-phase-8-beta-release-death-stroke.vercel.app/`.
- نشانی backend: `https://moamin9-chinverse-api.hf.space`.
- branch/HEAD محلی: `codex/phase-8-beta-release` /
  `b90c957b208d4e423eb3ebe15d21eafcb9ae62fc`؛ یکسان‌بودن نسخهٔ local و
  سرویس‌ها ادعا نمی‌شود. SHA frontend پشت حفاظت Vercel هنوز خوانده نشده است.
- `REV-002`: در دامنهٔ بازبینی تأکید شد؛ signup تازه یا تنظیمات live مربوط به
  verification در این نوبت آزموده/خوانده نشده‌اند.
- `REV-004`: تغییری نکرد؛ برای مرحلهٔ آمادگی انتشار باز است.
- مورد جدیدی از خرابی رابط مشاهده نشده است؛ هدایت ناشناس به Vercel، به‌خودی‌خود
  خرابی Chinverse نیست. بازبینی چشمی هنوز دریافت نشده است.
- آموزش انجام‌شده: frontend/backend/DB و رسانه؛ local/staging/deploy؛ تفاوت
  ورود محیط Vercel با ورود حساب Chinverse. پاسخ تمرین هنوز دریافت نشده است.
- اصلاح برنامه و تست رگرسیون: انجام نشده؛ این بخش خواندن کد، probe فقط‌خواندنی
  و به‌روزرسانی گزارش‌ها بود. build یا CI تازه‌ای اجرا نشده است.
- قدم بعدی: دریافت دستگاه‌ها و نام صفحهٔ نخست staging؛ سپس دستیار نسخهٔ
  frontend را در نشست مجاز بررسی کند و بازدید خانه/فرم ورود را هدایت کند.

## اصلاح جهت جلسهٔ ۱ — یادگیری جامع همراه با اصلاح

- تاریخ: ۲۰۲۶-۰۹-۲۰؛ توضیح تازهٔ صاحب پروژه بر اولویت یادگیری کل پروژه، علت
  انتخاب‌ها، وضعیت کار و جزئیات کد تأکید دارد. مثال‌های او فهرست محدود موضوع‌ها نیستند.
- روش فعال: تصویر کلی، سپس صفحهٔ واقعی و کد مرتبط؛ هنگام مشاهدهٔ مشکل،
  توضیح علت، اصلاح فنی دستیار، تست متناسب و بازآزمایی صاحب پروژه.
- شروع عملی انتخاب‌شده: خانه؛ دو تب «فعالیت‌ها» و «روند یادگیری» و متغیر
  `activeTab` در `frontend/src/app/page.tsx`، سپس قاب مشترک، استایل و API.
- محتوای مقدماتی: نقش frontend/backend/DB، Vercel/HF/Neon، Git/GitHub/Actions،
  Docker و Sentry؛ local/staging/production و جایگاه فعلی پیش از انتشار عمومی.
  توضیح نقش فعلی یک ابزار، اثبات انگیزهٔ تاریخی انتخاب آن نیست.
- فهم صاحب پروژه: هنوز ارزیابی/تأیید نشده؛ معرفی مفهوم با تسلط یکی نیست.
- مشاهدهٔ چشمی تازه: دریافت نشده؛ باگ جدید یا pass جدید ثبت نشده است.
- بررسی فنی: خواندن source و گزارش‌ها؛ مقایسهٔ `54973b8..b90c957` فقط چهار
  فایل گزارش/evidence را نشان می‌دهد و تغییر کد برنامه‌ای در این بازه نیست.
  SHA frontend اینترنتی همچنان در این نوبت مشاهده نشده است.
- وضعیت سرویس‌ها: ادعای تازهٔ آنلاین نداریم؛ به probe نوبت اول در ساعت ۱۰:۰۲ UTC
  ارجاع داده می‌شود. پنل‌های Neon/Sentry/Vercel در این بخش باز نشده‌اند.
- اصلاح برنامه و تست: انجام نشده؛ دو فایل راهنما و دفتر بازبینی به‌روز شدند.
- `REV-001` تا `REV-004` باز می‌مانند؛ بازتنظیم روش آموزش باگ محصول محسوب نمی‌شود.
- قدم بعدی صاحب پروژه: مشاهدهٔ صفحهٔ نخست staging و اعلام خانه/ورود Chinverse/
  ورود Vercel؛ پرسش کلی دستگاه‌ها تکرار نشود.
- قدم بعدی دستیار: از همان صفحه، ظاهر را به فایل و متغیر مربوط وصل کند؛ برای
  مورد قابل‌بازتولید، اصلاح کوچک و بازآزمایی انجام دهد و یادگیری را ثبت کند.

## مشاهدهٔ صاحب پروژه از خانه — ۲۰۲۶-۰۹-۲۱

- تصویر Chrome دسکتاپ از نشانی Preview ثبت‌شده: خانهٔ Chinverse باز است؛
  ورود Vercel در این تصویر دیده نمی‌شود. تب «فعالیت‌ها» انتخاب شده و پیام
  «هنوز فعالیتی ثبت نشده» نشان داده می‌شود. نوار پایین و دکمهٔ پشتیبانی دیده شدند.
- بررسی مستقل backend: `/api/v1/feed?limit=20` در `12:59:55Z` پاسخ `200 []`
  داد. این با تصویر سازگار است، اما پاسخ Network همان مرورگر تأیید نشده است.
  فید این endpoint فقط گالری و خدمات کاربران فعال/تأییدشده را جمع می‌کند؛
  خالی‌بودن آن وضعیت کل دیتابیس یا کاتالوگ درس را ثابت نمی‌کند.
- قاب باریک وسط صفحه از CSS با `max-width: 430px` می‌آید؛ رفتار کد فعلی است،
  پیشنهاد یا ایراد ظاهری صاحب پروژه دربارهٔ آن هنوز دریافت نشده است.
- `REV-005` در خواندن کد کشف و به‌صورت محلی اصلاح شد. تغییر فعلی در staging
  دیده نمی‌شود؛ تست فنی و تأیید چشمی جدا ثبت شدند.
- مطلب آموزش‌داده‌شده: تفاوت صفحهٔ خانه (`page.tsx`)، قاب مشترک (`AppShell`)،
  نوار مسیرها (`BottomNav`)، state تب و پاسخ endpoint فید.
- قدم بعد: تب «روند یادگیری» مشاهده شود؛ سپس برداشت صاحب پروژه از چیدمان
  دسکتاپ و جزئیات ظاهری همین خانه ثبت شود. SHA frontend زنده هنوز نامشخص است.

## مشاهدهٔ تب «روند یادگیری» — ۲۰۲۶-۰۹-۲۱

- صاحب پروژه تصویر مهمان از تب دوم را فرستاد و درست تشخیص داد که آمار
  شخصی بدون ورود در دسترس نیست.
- در تصویر، پیام خطای اتصال برای این حالت گمراه‌کننده بود؛ `REV-006` ثبت شد.
- endpoint بی‌حساب `401` و کد وابستگی به کاربر فعلی را تأیید کرد. این پاسخ
  با صفر بودن آمار کاربر یا خرابی پایگاه داده یکسان نیست.
- اصلاح محلی و تست فنی در بخش `REV-006` آمده؛ staging هنوز بازآزمایی نشده است.
- قدم بعدی آموزشی: مسیر «کاوش» و تفاوت دادهٔ عمومی با آمار وابسته به حساب.

## مشاهدهٔ «کاوش» و HSK — ۲۰۲۶-۰۹-۲۱

- تصاویر صاحب پروژه: `/explore` با گروه‌های متعدد و `/explore/hsk` با
  پیام نبود محتوا. کلیک و route موفق دیده شده‌اند؛ دوره/درس/پخش دیده نشد.
- دادهٔ عمومی backend: HSK و فهرست ۱۰۰ دورهٔ منتشرشده هر دو `200 []`؛
  taxonomy چهار گروه دارد. پاسخ همان مرورگر در DevTools ثبت نشده است.
- مفهوم آموزش: فهرست کارت‌های فرانت در کد ثابت است؛ محتوای دوره در DB نگه
  داشته می‌شود و API عمومی فقط `published` را نشان می‌دهد.
- اصلاح کد/تست تازه: انجام نشد. `REV-001` با شواهد تازه `P1` مسیر درس شد؛
  `REV-005/006` همچنان فقط اصلاح محلی‌اند.
- قدم بعدی آموزشی و عملی: چرخهٔ ساخت و انتشار محتوا، سپس یک درس نمونهٔ
  مجاز برای آزمون مسیر انتخاب تا پخش. تأیید ظاهر دسته‌های خالی با صاحب پروژه است.

## شرط صفحه‌های ویدیو — ۲۰۲۶-۰۹-۲۱

- صاحب پروژه ظاهر فعلی را تأیید کرد و قابل‌دیدن‌بودن صفحه‌های ویدیو را شرط
  بازبینی دانست. این نیاز در `REV-001` ثبت شد.
- کد مسیرهای دوره و پلیر بررسی شد، ولی staging بدون محتوای منتشرشده هنوز
  امکان بازدید واقعی آن‌ها را نمی‌دهد. نمونهٔ مصنوعی و بازآزمایی لازم است.
- هیچ اصلاح کد یا deploy در این بخش انجام نشد؛ آزمون Playwright تازه قطع شد
  و pass محسوب نمی‌شود.

## بازبینی بالای پنل ادمین — ۲۰۲۶-۰۹-۲۱

- سه تصویر صاحب پروژه نشان می‌دهند پنل `/admin` با دسترسی مدیر باز شده و تب
  «محتوا و ویدیو» و «دیکشنری» در دسترس‌اند. پیام بارگذاری ۳۰۰ کلمه دیده شد؛
  این تصویر شمار دوره‌های منتشرشده یا کارکرد پلیر را ثابت نمی‌کند.
- `REV-007` (`P2`): عنوان و توضیح سربرگ بین دکمهٔ بازگشت و چهار کنترل دیگر
  در قاب حداکثر ۴۳۰ پیکسلی فشرده می‌شدند؛ تب‌های افقی نیز در لبهٔ قاب قطع
  می‌شدند. علت از flex تک‌ردیفه و `overflow-x-auto` در `admin/page.tsx` است.
- اصلاح محلی: سربرگ دو ردیف شد (عنوان/بازگشت و کنترل‌ها) و تب‌ها به ردیف
  بعد می‌روند؛ برچسب ناحیهٔ ناوبری و وضعیت انتخاب دکمه‌ها هم اضافه شد.
- `npm run typecheck`، ESLint فایل و `npm run build` موفق بودند. آزمون
  Playwright موبایل در این محیط، هم با dev و هم با production server پس از
  شروع تست پاسخ نهایی نداد و قطع شد؛ **pass تصویری ادعا نمی‌شود**.
- هنوز commit/deploy و مشاهدهٔ اصلاح روی staging انجام نشده است. پس از
  استقرار، صاحب پروژه باید بالای پنل را در همان عرض ببیند و خوانایی عنوان،
  کنترل‌ها و هر پنج تب را تأیید کند.

### نتیجهٔ استقرار و بازآزمایی همین بخش

- commit `8e8ccd41dab4f119e5164c13428830a148b558ee` به شاخهٔ
  `codex/phase-8-beta-release` push شد. تغییرهای نامرتبط موجود در working
  tree وارد commit نشدند.
- [Quality gate push](https://github.com/MoAminPourzare/Chinverse/actions/runs/35608326573)،
  [استقرار backend](https://github.com/MoAminPourzare/Chinverse/actions/runs/35608326510)
  و [smoke exact-SHA](https://github.com/MoAminPourzare/Chinverse/actions/runs/35608326493)
  همگی `success` شدند؛ status عمومی Vercel برای همین commit نیز `success`
  بود. backend `/health` اکنون `status=ok`، `tier=staging` و همین SHA را
  گزارش می‌کند. frontend `/api/health` پشت نشست Vercel مستقیماً خوانده نشد؛
  نتیجهٔ smoke شواهد تطابق دو provider است.
- صاحب پروژه پس از `Ctrl+F5` پاسخ داد «بله، درست شده» و خوانایی عنوان و هر
  پنج تب را تأیید کرد. `REV-007` بسته شد. این تأیید به `REV-005/006` یا
  صفحهٔ پخش ویدیو تعمیم داده نمی‌شود.

## تکمیل ساختار HSK بر اساس مرجع صاحب پروژه — ۲۰۲۶-۰۹-۲۲

- نیاز تأییدشده: فقط یک fixture HSK کافی نیست؛ فهرست باید ۹ سطح استاندارد،
  صفحهٔ معرفی هر سطح و صفحهٔ قابل‌دیدن هر درس را داشته باشد، حتی وقتی فایل
  ویدیوی اصلی هنوز روی سرور نیست.
- پیاده‌سازی commit `94f74ca9cfdabe85a55d7dfea2782e07feb2e569`: کاتالوگ ۹ سطح، کاورهای
  موجود پروژه، معرفی/سطح/مخاطب، ۱۰ تا ۲۰ کارت درس مطابق کتاب و route مستقل
  هر درس با حالت صریح «ویدیو به‌زودی». مسیر DB عددی برای fixture واقعی حفظ شد.
- تست محلی: viewport موبایل ۳۹۰×۸۴۴، شمار ۹ کارت، ۱۵ درس HSK 1، صفحهٔ درس
  اول و کنترل قبلی/بعدی. مشکل خوانایی حالت تیره هنگام بازبینی پیدا و اصلاح شد.
  دو تست واحد، typecheck، lint بدون error و production build موفق‌اند.
- استقرار: هر دو Quality gate، backend deploy و exact-SHA smoke برای commit
  بالا `success` شدند. نسخهٔ زنده در viewport موبایل دقیقاً ۹ کارت نشان داد؛
  HSK 4下 ده درس پیوسته از `第11课（下）` و صفحهٔ درس اول قاب «ویدیو به‌زودی»
  و جابه‌جایی درس بعدی داشت. `/hsk/70` نیز همچنان دورهٔ ویدیویی آزمایشی و
  لینک‌های پلیر را نشان می‌دهد.
- وضعیت: آزمون فنی و staging موفق؛ تأیید چشمی صاحب پروژه از این طراحی هنوز
  جداگانه ثبت نشده است.
- مرز داده: این کاتالوگ «برنامهٔ دوره» است و ادعای وجود فایل ویدیو ندارد.
  وقتی ویدیوها روی سرور قرار گرفتند، هر lesson از مسیر media asset و مجوز
  موجود به فایل واقعی وصل می‌شود؛ کاور/زمان/زیرنویس جعلی ثبت نشده است.

## قالب ثبت اصلاح و بازآزمایی

```text
شناسه:
اولویت قطعی و دلیل:
بازتولید روی کدام محیط/نسخه/حساب آزمایشی:
علت:
تصمیم محصول (اگر لازم بود):
فایل اصلاح‌شده و commit/deploy:
تست خودکار و نتیجه:
بازآزمایی چشمی صاحب پروژه:
مفهوم یادگرفته‌شده:
وضعیت بعدی:
```

## اتصال کاتالوگ HSK به انتشار واقعی — ۲۰۲۶-۰۹-۲۲

- `REV-001` باز است: پخش مهمان با fixture 70 آزموده شده، اما ۹ جلد جدید هنوز ویدیوی منتشرشدهٔ منطبق و تأیید چشمی صاحب پروژه ندارند.
- ایراد تکمیلی: کارت‌های برنامه‌ریزی‌شده پس از انتشار ویدیو به پلیر نمی‌رفتند. اصلاح در commitهای `653018d77c7b4533bdb00c8ec0419ae98d2e6c94` و `fe743100a07c3475ada328c6312d9f97db0c0e89`: تطبیق exact slug و `lesson_index` یکتا، لینک پخش، تفکیک بارگذاری/خطا و حذف امتیاز/پیشرفت ساختگی.
- تست: ۴ تست کاتالوگ/تطبیق، typecheck، lint مرتبط و production build موفق. انتشار یک درس منطبق در staging هنوز بازآزمایی نشده است؛ API عمومی فقط fixture قبلی با slug جدا را دارد.
- استقرار نهایی: هر دو Quality gate (`35723282497`/`35723277126`)، backend deploy (`35723277311`) و exact-SHA smoke (`35723277007`) success؛ Vercel success و backend health با SHA `fe743100a07c3475ada328c6312d9f97db0c0e89` و readiness سالم. بازدید صاحب پروژه هنوز در انتظار ثبت است. بازآزمایی بصری شامل فهرست ۹ سطح، صفحهٔ HSK 1 و صفحهٔ درس اول آن است.
- مفهوم آموزش: کاتالوگ فرانت طرح مسیر است؛ دیتابیس/رسانه منبع حقیقت ویدیوست. `lesson_index` جایگاه در همان جلد است؛ برای نیمهٔ پایین از ۱ شروع می‌شود، حتی اگر شمارهٔ چاپی درس ۱۱ یا ۲۱ باشد.

## بازبینی بخش «تلفظ» — محلی، ۲۰۲۶-۰۹-۲۲

- درخواست صاحب پروژه: سه دوره و صفحه‌های معرفی/درس مطابق چهار تصویر مرجع آماده شوند؛ تغییرها تا پایان بازبینی سایر بخش‌ها محلی بمانند و یک‌بار deploy شوند. هیچ commit، push یا deploy برای این بخش انجام نشد.
- وضعیت پیشین: route تلفظ فقط دورهٔ منتشرشدهٔ API را می‌خواند و در نبود ویدیو فهرست مرجع سه‌تایی دیده نمی‌شد. اکنون کاتالوگ برنامه‌ریزی‌شدهٔ Yoyo (۲۷ درس + ۴ تمرین)، Grace (۱۴ درس) و Yang (۱۹ درس) با عکس‌های tracked موجود ساخته شد؛ صفحهٔ معرفی و route هر درس اضافه شد. مسیر عددی DB حفظ شد.
- واقعیت داده: تصاویر پوشهٔ صاحب پروژه با دارایی‌های tracked برابر بودند؛ برای نام درس‌ها فقط موارد خوانا در مرجع به کار رفت. امتیاز چهارستاره، نوار پیشرفت و زمان ویدیو به‌عنوان دادهٔ واقعی نمایش داده نشدند. تمرین‌های Yoyo هنوز محتوای تعریف‌شده ندارند و تنها شمار برنامه‌ای‌شان دیده می‌شود.
- آزمون محلی: viewport ۳۹۰×۸۴۴، سه کارت، ۲۷/۱۴/۱۹ لینک، و درس اول Yoyo دیده شد؛ فاصلهٔ نامناسب متن فارسی در معرفی اصلاح شد. ۴ تست هدفمند، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند. تأیید چشمی/متنی صاحب پروژه هنوز دریافت نشده است.
- اتصال واقعی رسانه نیازمند دورهٔ منتشرشده با slug دقیق و درس منتشرشده با `lesson_index` یکتا است؛ در این نوبت هیچ محتوایی در staging منتشر نشد. مجوز تصاویر نیز برای لانچ عمومی هنوز `review_required` است.

## بازبینی بخش «کاراکتر» — محلی، ۲۰۲۶-۰۹-۲۲

- پنج تصویر مرجع سه دورهٔ Yoyo (۲۰ درس)، Grace (۷ درس) و 百家Talk (۸ درس) را مشخص کردند. فهرست سه‌تایی، معرفی هر دوره و route مستقل همهٔ درس‌ها در فرانت آماده شد. عکس‌های موجود پروژه همان عکس‌های پوشهٔ صاحب پروژه‌اند؛ نسخهٔ تکراری ثبت نشد.
- عنوان/زیرعنوان فقط در حد خوانا بودن مرجع ثبت شد: Grace درس ۱ «How to learn Chinese» و 百家Talk درس ۱ «引言». برای سایر درس‌ها شمارهٔ عمومی نمایش داده می‌شود. متن معرفی پیش‌نویس نزدیک به مرجع است و هنوز نیاز به تأیید محتوایی صاحب پروژه دارد. امتیاز چهارستاره، نوار پیشرفت و زمان ویدیو دادهٔ واقعی ندارند و ساخته نشدند.
- کاتالوگ کاراکتر با API منتشرشده تنها از طریق زیرمجموعه و slug دقیق و `lesson_index` یکتای درس اتصال می‌یابد؛ تا انتشار واقعی، کارت درس صفحهٔ «ویدیو هنوز منتشر نشده» را نشان می‌دهد. قالب مشترک تلفظ/کاراکتر ساخته شد و مسیر عددی دوره‌های قدیمی باقی ماند.
- آزمون محلی ۳۹۰×۸۴۴: هر سه تصویر بارگذاری شد، سه کارت و ۲۰/۷/۸ لینک درس حاضر بودند، سرریز افقی نبود، درس اول 百家Talk و لینک درس بعدی کار کردند. فهرست تلفظ نیز پس از تغییر قالب سه کارت داشت. ۶ تست هدفمند، typecheck، lint مرتبط و production build موفق؛ بازبینی چشمی صاحب پروژه و مجوز عکس‌ها (`review_required`) باز است.
- طبق تصمیم صاحب پروژه هیچ commit، push یا deploy انجام نشد؛ staging فعلاً این تغییر را ندارد.

## بازبینی بخش «گرامر» — محلی، ۲۰۲۶-۰۹-۲۲

- فهرست چهار دورهٔ مرجع با عکس‌های موجود پروژه و ۱۷۵/۷/۱۰/۷ جای درس ساخته شد. صفحهٔ معرفی و route مستقل همهٔ درس‌ها آماده‌اند. دادهٔ منتشرشده تنها با زیرمجموعهٔ `grammar`، slug دقیق و `lesson_index` یکتا به پلیر وصل می‌شود؛ ویدیوی غایب با حالت صریح نشان داده می‌شود. مسیر عددی قبلی محفوظ است.
- ابهام باز: کارت چهارم فهرست `听故事学成语` و عکس Grace دارد، اما صفحهٔ جزئیاتِ ارسالی `Grace Mandarin (Grammar)` است. تا پاسخ صاحب پروژه، عنوان Grace با صفحهٔ جزئیات هماهنگ شده است. متن معرفی پیش‌نویس است؛ ستاره، نوار پیشرفت و مدت بدون دادهٔ واقعی نمایش داده نشدند.
- آزمون محلی در ۳۹۰×۸۴۴: چهار کارت و عکس سالم، چهار صفحه با ۱۷۵/۷/۱۰/۷ لینک درس، بدون سرریز افقی، درس آخر دورهٔ ۱۷۵تایی و حالت پایان دوره. ۸ تست هدفمند، typecheck، lint مرتبط و production build موفق. بازبینی چشمی صاحب پروژه، تصمیم عنوان چهارم و مجوز عکس‌ها (`review_required`) باز مانده‌اند.
- هیچ commit، push یا deploy انجام نشد؛ staging این تغییر را ندارد.

## بازبینی «اصطلاحات و ضرب‌المثل‌های ادبی» — محلی، ۲۰۲۶-۰۹-۲۲

- پنج دورهٔ مرجع با عکس‌های موجود و ۸۵/۵۰/۲۰/۵/۳۳ جای درس در فهرست اضافه شدند. هر دوره صفحهٔ معرفی و هر درس route مستقل دارد؛ دادهٔ واقعیِ منتشرشده تنها با زیرمجموعهٔ `idioms`، slug دقیق و `lesson_index` یکتا به پلیر وصل می‌شود. در نبود ویدیو، وضعیت منتشرنشده صریح است. مسیر عددی قبلی باقی ماند.
- آزمون محلی ۳۹۰×۸۴۴: پنج عکس سالم، پنج کارت، همهٔ ۱۹۳ لینک درس، نبود سرریز افقی، صفحهٔ درس ۳۳ دورهٔ آخر و وضعیت پایان دوره. سربرگ بلند ابتدا دوخطی بود و به حالت یک‌خطی نزدیک مرجع اصلاح و دوباره دیده شد. ۱۰ تست هدفمند کاتالوگ‌ها، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند.
- متن معرفی بر پایهٔ مرجع پیش‌نویس است؛ عنوان‌های واقعی درس، رسانه، مدت، امتیاز و پیشرفت هنوز دادهٔ تأییدشده ندارند. بازبینی چشمی/محتوایی صاحب پروژه و مجوز عکس‌ها (`review_required`) باز است. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «چینی کاربردی» — محلی، ۲۰۲۶-۰۹-۲۲

- سه دورهٔ مرجع با ۹/۶/۶۹ جای درس به فهرست افزوده شدند. دو دورهٔ Hoa Ngữ Nam Khánh از لوگوی موجود `fMEg1uHEG.jpeg` و Love Chinese از عکس موجود خودش استفاده می‌کنند؛ هر دو با پوشهٔ صاحب پروژه برابرند. صفحهٔ معرفی و route مستقل هر درس ساخته شد. نام درس اول دورهٔ اول از مرجع ثبت شد؛ برای درس‌های نامعلوم عنوان حدسی ساخته نشد.
- اتصال ویدیوی واقعی نیازمند زیرمجموعهٔ `practical`، slug دقیق و `lesson_index` یکتای درس منتشرشده است؛ تا آن زمان صفحهٔ درس وضعیت منتشرنشده دارد. مسیر عددی قدیمی محفوظ است و امتیاز/مدت/پیشرفت ساختگی نمایش داده نمی‌شود.
- آزمون محلی ۳۹۰×۸۴۴: سه عکس سالم، سه کارت بدون سرریز، صفحه‌های ۹/۶/۶۹ درسی و درس اول NK با عنوان `第1集` و زیرعنوان `公司里常用句型 01~05`. متن کارت‌ها برای خوانایی نام کامل کمی ریزتر شد و دوباره دیده شد. ۱۲ تست هدفمند، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند.
- بازبینی چشمی/محتوایی صاحب پروژه و مجوز عکس‌ها (`review_required`) باز است. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «یادگیری با ولاگ» — محلی، ۲۰۲۶-۰۹-۲۲

- پنج دورهٔ مرجع با ۵۵/۲۰/۱۴/۱۹/۲۲ جای درس در فهرست افزوده شدند. دو دورهٔ Zhangkai عکس مشترک دارند ولی مسیرهای جدا هستند؛ عکس‌های Zhangkai، HongCha، Free To Learn و Hi از دارایی‌های موجود پروژه‌اند. هر دوره صفحهٔ معرفی و هر درس route مستقل دارد.
- تا انتشار واقعی، صفحهٔ درس وضعیت منتشرنشده را نشان می‌دهد. اتصال به پلیر تنها با زیرمجموعهٔ `vlogs`، slug دقیق و `lesson_index` یکتای درس منتشرشده انجام می‌شود؛ مسیر عددی قبلی باقی ماند. عنوان‌های نامعلوم درس، مدت، امتیاز و پیشرفت جعل نشدند.
- آزمون محلی ۳۹۰×۸۴۴: پنج کارت و عکس‌های سالم، ۱۳۰ لینک درس، نبود سرریز افقی، صفحهٔ درس آخر Zhangkai کوتاه و حالت پایان دوره. ۱۴ تست هدفمند، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند.
- متن معرفی و ظاهر نهایی نیاز به بازبینی صاحب پروژه دارند؛ مجوز عکس‌ها (`review_required`) باز است. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «زبان چینی کلاسیک» — محلی، ۲۰۲۶-۰۹-۲۳

- یک دورهٔ مرجع 百家Talk (古代汉语入门) با هشت جای درس به زیرمجموعهٔ `classical` افزوده شد؛ با «شعر و ادبیات کلاسیک» ادغام نشد. عکس موجود پروژه استفاده شد و صفحهٔ معرفی و route مستقل هر درس آماده‌اند.
- اتصال پلیر نیازمند دورهٔ منتشرشده با slug دقیق و درس دارای `lesson_index` یکتا در همان زیرمجموعه است. تا آن زمان صفحهٔ درس وضعیت منتشرنشده دارد و مسیر عددی قدیمی حفظ می‌شود. عنوان درس، مدت، امتیاز و پیشرفت بدون دادهٔ واقعی نمایش داده نشدند.
- آزمون محلی ۳۹۰×۸۴۴: یک کارت و عکس سالم، هشت لینک درس، نبود سرریز، صفحهٔ درس هشتم و حالت پایان دوره. ۱۸ تست هدفمند، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند.
- متن معرفی و ظاهر نهایی نیاز به بازبینی صاحب پروژه دارند؛ مجوز عکس (`review_required`) باز است. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «واژگان هم‌معنی» — محلی، ۲۰۲۶-۰۹-۲۳

- سه دورهٔ مرجع با ۲۰/۵/۳۷ جای درس در فهرست افزوده شدند. عکس‌های 百家Talk، Q&A Mandarin و Free To Learn از دارایی‌های موجود پروژه‌اند. هر دوره صفحهٔ معرفی و هر درس route مستقل دارد؛ دادهٔ منتشرشده فقط با زیرمجموعهٔ `synonyms`، slug دقیق و `lesson_index` یکتا به پلیر متصل می‌شود.
- ایراد پیدا و اصلاح‌شده: نام فایل `Q&A Mandarin.jpeg` به علت `&` در URL، 404 می‌داد؛ مسیر با `Q%26A%20Mandarin.jpeg` امن شد و بارگذاری مستقیم و داخل `next/image` موفق بود.
- آزمون محلی ۳۹۰×۸۴۴: سه عکس سالم، سه کارت، ۶۲ لینک درس، بدون سرریز، صفحهٔ درس ۳۷ و حالت پایان دوره. ۱۶ تست هدفمند، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند. عنوان‌های نامعلوم درس و داده‌های مدت/امتیاز/پیشرفت جعل نشدند.
- متن معرفی و ظاهر نهایی نیاز به بازبینی صاحب پروژه دارند؛ مجوز عکس‌ها (`review_required`) باز است. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «فیلم» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست مستقل هشت‌فیلمه مطابق مرجع ساخته شد: 我不是药神، دو قسمت 流浪地球، 一秒钟، 你好，李焕英، 西虹市首富، 红海行动 و 我的少女时代. کارت‌ها سال انتشار و پوستر موجود پروژه را نشان می‌دهند؛ صفحهٔ هر فیلم خلاصه، ژانر، کشور/سال، کارگردان و بازیگران اصلی دارد.
- امتیاز و مدت نامعلومِ تصویرها جعل نشد. اتصال پخش فقط وقتی فعال می‌شود که API عمومی یک رکورد `movies` با slug دقیق و درس دارای `media_id` برگرداند؛ در غیر این صورت پیام منتشرنشدن نسخهٔ ویدیویی دیده می‌شود. fallback مسیر عددی قدیمی حفظ شده است.
- خطای URL پوستر 一秒钟 در بازبینی نخست کشف شد. فایل دارای چند `&` با نام امن `one-second-poster.webp` جایگزین شد و پوستر عمودی درست 流浪地球1 انتخاب شد. بازآزمایی موبایل ۳۹۰×۸۴۴: هشت کارت، هشت پوستر سالم، همهٔ صفحه‌های جزئیات، چهار برچسب اطلاعات فیلم و نبود سرریز افقی موفق بود.
- ۱۹ تست هدفمند در ۹ فایل، typecheck، lint مرتبط و production build موفق‌اند. متن‌های فارسی و مجوز پوسترها (`review_required`) هنوز بازبینی صاحب پروژه می‌خواهند. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «سریال» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست پنج سریال مرجع با شمار ۲۵/۴۰/۳۶/۱۵/۳۲ قسمت ساخته شد. پوسترهای موجود پروژه با مرجع تطبیق داده شدند و صفحهٔ هر سریال، Pinyin، خلاصه، ژانر، سال/کشور، کارگردان و بازیگران را همراه شبکهٔ قسمت‌ها نمایش می‌دهد.
- امتیاز و مدت نامشخص جعل نشد. قسمت واقعی تنها با زیرمجموعهٔ `series`، slug دقیق، `media_id` و index یکتای قسمت (`episode_index` یا `lesson_index`) به پلیر متصل می‌شود؛ کارت‌های بدون رسانه وضعیت «منتشر نشده» دارند و مسیر عددی قدیمی همچنان کار می‌کند.
- قالب مشترک رسانهٔ نمایشی برای فیلم/سریال ایجاد شد. بازآزمایی ۳۹۰×۸۴۴: پنج کارت و پوستر سالم، پنج صفحهٔ جزئیات، چهار برچسب اطلاعات، دقیقاً ۲۵/۴۰/۳۶/۱۵/۳۲ کارت قسمت و بدون سرریز افقی یا خطای تصویر. رگرسیون فهرست فیلم نیز هشت کارت و هشت پوستر سالم داشت.
- ۲۲ تست هدفمند در ۱۰ فایل، typecheck، lint مرتبط، production build و `git diff --check` موفق‌اند. متن‌های فارسی و مجوز پوسترها (`review_required`) هنوز بازبینی صاحب پروژه می‌خواهند. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «موسیقی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست مستقل چهار هنرمند مطابق مرجع ساخته شد: 周杰伦 (Jay Chou)، 邓紫棋 (G.E.M.)، 林俊杰 (JJ Lin) و شخصیت موسیقایی هوش مصنوعی 阿若 Auro. سه تصویر نخست از دارایی‌های موجود پروژه انتخاب شدند؛ تصویر Auro از قاب مرجع ارسالی صاحب پروژه استخراج شد. نام ناامن تصویر G.E.M. نیز با نسخهٔ `gem-profile.jpg` برای URL عمومی جایگزین شد.
- هر هنرمند صفحهٔ مستقل با نام چینی، Pinyin، بیوگرافی فارسی و سبک‌های موسیقی دارد. Auro صریحاً به‌عنوان هویت هنری دیجیتال معرفی شده است. ستاره و مدت نامشخصِ تصاویر جعل نشدند.
- اگر API عمومی در زیرمجموعهٔ `music` دوره‌ای با slug دقیق هنرمند و حداقل یک درس دارای `media_id` برگرداند، دکمهٔ «شنیدن آثار» به همان درس در پلیر وصل می‌شود و ذخیره‌سازی نیز فعال خواهد شد. تا آن زمان وضعیت منتشرنشدن اثر روشن است؛ مسیر عددی قبلی حفظ شده است.
- بازبینی ۳۹۰×۸۴۴: چهار کارت و چهار تصویر سالم، چهار صفحهٔ جزئیات با بخش‌های بیوگرافی و سبک، بدون سرریز افقی، خطای جاوااسکریپت یا پاسخ ناموفق تصویر. ۲۷ تست هدفمند در ۱۲ فایل، typecheck، lint مرتبط و production build موفق‌اند. متن‌های فارسی و مجوز تصاویر (`review_required`) هنوز بازبینی صاحب پروژه می‌خواهند. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «انیمیشن و کارتون» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست مستقل هشت‌تایی مطابق مرجع ساخته شد: 哪吒之魔童降世، 姜子牙، 大护法، 白蛇：缘起، 白蛇2：青蛇劫起، 大鱼海棠، 熊出没之探险日记 و 《中华弟子规》第一季. شش عنوان نخست سال انتشار و دو مجموعهٔ پایانی به‌ترتیب ۵۲ و ۶۰ قسمت را نشان می‌دهند؛ هر هشت تصویر از دارایی‌های موجود پروژه انتخاب شدند.
- صفحهٔ هر عنوان شامل Pinyin، پوستر، خلاصه، ژانر، سال/کشور و اطلاعات تولید منطبق با مرجع است. قالب مشترک رسانه توسعه یافت تا برچسب‌هایی مانند «نام واقعی» و «تولیدکننده» را نمایش دهد و بخش نامرتبط بازیگران به انیمیشن تحمیل نشود. امتیاز و مدت نامشخص جعل نشدند.
- برای شش فیلم، نخستین درس منتشرشدهٔ دارای `media_id` به پلیر وصل می‌شود. برای دو مجموعه، فقط درس دارای `media_id` و `episode_index` یا `lesson_index` یکتای همان قسمت فعال می‌شود؛ بقیه صریحاً «منتشر نشده» می‌مانند. مسیر عددی قبلی حفظ شده است.
- دکمهٔ نشان‌کردن به قالب مشترک فیلم/سریال/انیمیشن اضافه شد: فقط پس از وجود رکورد منتشرشده فعال می‌شود. بازبینی ۳۹۰×۸۴۴ هر هشت کارت، هشت تصویر و هشت صفحهٔ جزئیات را بدون سرریز افقی نشان داد و شبکه‌های دو مجموعه دقیقاً ۵۲ و ۶۰ کارت داشتند.
- تست‌های کاتالوگ و رگرسیون، typecheck، lint مرتبط و production build موفق‌اند. اتصال زندهٔ رسانه به علت خاموش‌بودن بک‌اند محلی اجرا نشد و حالت خطا در رابط صریح ماند. متن‌های فارسی و مجوز تصاویر (`review_required`) هنوز بازبینی صاحب پروژه می‌خواهند. هیچ commit، push یا deploy انجام نشد؛ staging تغییر نکرده است.

## بازبینی «گفتارهای موضوعی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست سه مجموعهٔ مرجع به‌ترتیب 两娃妈夏博士 Dr. Yuni Xia، 纪实说 و 纪实文学馆 با ۱۰۶، ۴۴ و ۲۱ گفتار ساخته شد. هر سه تصویر دقیق از دارایی‌های موجود پروژه انتخاب شدند و در فهرست و صفحهٔ معرفی سالم بارگذاری شدند.
- هر مجموعه صفحهٔ مستقل با بخش‌های «معرفی پادکست»، «سطح» و «گفتارهای مجموعه» دارد. برای هر گفتار route مستقل ساخته شد؛ چون عنوان، مدت و امتیاز واقعی گفتارها در مرجع مشخص نبود، فقط شمارهٔ «گفتار» نمایش داده می‌شود و دادهٔ حدسی ساخته نشد.
- گفتار منتشرشده تنها وقتی به پلیر وصل می‌شود که API عمومی یک دوره در زیرمجموعهٔ `topic-talks` با slug دقیق و درس دارای `media_id` و `metadata_json.lesson_index` یکتای همان جایگاه برگرداند. نشان‌کردن نیز پس از وجود دورهٔ منتشرشده فعال می‌شود و مسیر عددی قدیمی همچنان fallback است.
- بازبینی موبایل ۳۹۰×۸۴۴: سه کارت و تصویر سالم، سه صفحهٔ معرفی، دقیقاً ۱۰۶/۴۴/۲۱ لینک گفتار، گفتار ۱۰۶ و حالت «پایان مجموعه»، و نبود سرریز افقی. مجموعهٔ کامل ۱۳۲ تست در ۴۵ فایل، typecheck، lint مرتبط و production build موفق‌اند. پیش‌نمایش در `topic-talks-grid-390x844.png` ثبت شد.
- متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «آشپزی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست چهار برنامهٔ مرجع به‌ترتیب 万能工具人阿伟، 美食作家王刚، 老饭骨 و 中国美食频道 با ۴۶، ۱۶، ۲۱ و ۳۴ قسمت ساخته شد. هر چهار تصویر دقیق از دارایی‌های موجود پروژه انتخاب شدند و در فهرست و صفحهٔ معرفی سالم بارگذاری شدند.
- هر برنامه صفحهٔ مستقل با نام چینی، Pinyin، بخش‌های «معرفی برنامه»، «سطح» و «قسمت‌های برنامه» دارد. برای هر قسمت route مستقل ساخته شد؛ چون عنوان، مدت و امتیاز واقعی قسمت‌ها در مرجع مشخص نبود، فقط شمارهٔ «قسمت» نمایش داده می‌شود و دادهٔ حدسی ساخته نشد.
- قسمت منتشرشده تنها با دورهٔ API در زیرمجموعهٔ `cooking`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود دورهٔ منتشرشده فعال می‌شود و مسیر عددی قبلی همچنان fallback است.
- بازبینی موبایل ۳۹۰×۸۴۴: چهار کارت و تصویر سالم، چهار صفحهٔ معرفی با Pinyin جدا، دقیقاً ۴۶/۱۶/۲۱/۳۴ لینک قسمت، قسمت ۴۶ و حالت «پایان مجموعه»، و نبود سرریز افقی. مجموعهٔ کامل ۱۳۵ تست در ۴۶ فایل، typecheck، lint مرتبط و production build موفق‌اند. پیش‌نمایش در `cooking-grid-390x844.png` ثبت شد.
- API محلی دادهٔ منتشرشده هنگام بازبینی در دسترس نبود؛ بنابراین اتصال زندهٔ پخش و ذخیره‌سازی اجرا نشد و رابط حالت آماده‌سازی را صریح نشان داد. متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «هنرهای رزمی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست سه دورهٔ مرجع به‌ترتیب 学国学网（八式太极拳）、Lee Wushu 武者劲松（基础棍术 教学） و 立新舞太极 Taichi Wei（太极功夫扇） با ۱۳، ۱۰ و ۱۳ قسمت ساخته شد. تصاویر دقیق موجود پروژه برای هر سه کارت استفاده شدند.
- مرجع برای دورهٔ اول دو تصویر متفاوت دارد: لوگوی `学国学网` در فهرست و تصویر اجرای `八式太极拳` در صفحهٔ جزئیات. قالب مشترک دوره‌ها با تصویر جزئیات اختیاری توسعه یافت تا این تفاوت دقیقاً حفظ شود؛ بخش‌های قبلی در نبود این مقدار همان تصویر اصلی خود را نمایش می‌دهند.
- هر دوره صفحهٔ مستقل با بخش‌های «معرفی دوره»، «سطح» و «قسمت‌های دوره» و هر قسمت route مستقل دارد. نام، مدت و امتیاز واقعی قسمت‌ها در مرجع مشخص نبود؛ بنابراین فقط شمارهٔ «قسمت» نمایش داده می‌شود و دادهٔ حدسی ساخته نشد.
- قسمت منتشرشده تنها با دورهٔ API در زیرمجموعهٔ `martial-arts`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود دورهٔ منتشرشده فعال می‌شود و مسیر عددی قبلی همچنان fallback است.
- بازبینی موبایل ۳۹۰×۸۴۴: سه کارت و تصویر سالم، سه صفحهٔ معرفی، تفاوت درست تصویر فهرست/جزئیات دورهٔ اول، دقیقاً ۱۳/۱۰/۱۳ لینک قسمت، قسمت ۱۳ و حالت «پایان مجموعه»، و نبود سرریز افقی. مجموعهٔ کامل ۱۳۸ تست در ۴۷ فایل، typecheck، lint مرتبط و production build موفق‌اند. پیش‌نمایش در `martial-arts-grid-390x844.png` ثبت شد.
- API محلی محتوای منتشرشده هنگام بازبینی در دسترس نبود؛ اتصال زندهٔ پخش و ذخیره‌سازی اجرا نشد. متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «تمرینات انرژی و سلامت (چی‌گونگ)» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست دو دورهٔ مرجع Shi Heng Yi Online（What is Qi Gong?） و 学国学网《八段锦》 با ۱۱ و ۲۰ تمرین ساخته شد. عنوان صفحه با زیرعنوان دوخطی «(气功 چی‌گونگ)» مطابق مرجع نمایش داده می‌شود.
- تصویر Shi Heng Yi و لوگوی `学国学网` از دارایی‌های موجود پروژه‌اند. برای بادوان‌جین، لوگو در فهرست و پوستر آموزشی افقی `《八段锦》` در صفحهٔ جزئیات نمایش داده می‌شود؛ قابلیت تصویر جزئیات اختیاریِ قالب مشترک این تفاوت را حفظ می‌کند.
- هر دوره صفحهٔ مستقل با بخش‌های «معرفی دوره»، «سطح» و «تمرین‌های دوره» و هر تمرین route مستقل دارد. نام، مدت و امتیاز واقعی تمرین‌ها در مرجع مشخص نبود؛ بنابراین فقط شمارهٔ «تمرین» نمایش داده می‌شود و دادهٔ حدسی ساخته نشد.
- تمرین منتشرشده تنها با دورهٔ API در زیرمجموعهٔ `energy-health`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود دورهٔ منتشرشده فعال می‌شود و مسیر عددی قبلی همچنان fallback است.
- بازبینی موبایل ۳۹۰×۸۴۴: عنوان دوخطی، دو کارت و تصویر سالم، دو صفحهٔ معرفی، تفاوت درست تصویر فهرست/جزئیات بادوان‌جین، دقیقاً ۱۱/۲۰ لینک تمرین، تمرین ۲۰ و حالت «پایان مجموعه»، و نبود سرریز افقی. مجموعهٔ کامل ۱۴۱ تست در ۴۸ فایل، typecheck، lint مرتبط و production build موفق‌اند. پیش‌نمایش در `energy-health-grid-390x844.png` ثبت شد.
- API محلی محتوای منتشرشده هنگام بازبینی در دسترس نبود؛ اتصال زندهٔ پخش و ذخیره‌سازی اجرا نشد. متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «خطاطی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست سه دورهٔ مرجع استاد 陳忠建 به‌ترتیب 零基础自学书法入门، 欧阳询(结构) و 颜真卿(多宝塔碑) با ۱۴، ۳۶ و ۲۳ درس ساخته شد. هر سه کارت و صفحهٔ جزئیات از تصویر مشترک دقیق موجود پروژه استفاده می‌کنند و عنوان استاد از عنوان دوره جدا نمایش داده می‌شود.
- برای هر دوره معرفی فارسی اختصاصی، سطح، فهرست کامل درس‌ها و route مستقل هر درس آماده شد. چون نام، مدت و امتیاز واقعی درس‌ها در مرجع مشخص نبود، فقط شمارهٔ «درس» نمایش داده می‌شود و دادهٔ حدسی ساخته نشد.
- درس منتشرشده تنها با دورهٔ API در زیرمجموعهٔ `calligraphy`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود دورهٔ منتشرشده فعال می‌شود و مسیر عددی قبلی همچنان fallback است.
- بازبینی موبایل ۳۹۰×۸۴۴: سه کارت و تصویر سالم، سه زیرعنوان چینی کامل، سه صفحهٔ معرفی، دقیقاً ۱۴/۳۶/۲۳ لینک درس، درس ۲۳ و حالت «پایان مجموعه»، بدون سرریز افقی یا خطای کنسول. مجموعهٔ کامل ۱۴۵ تست در ۴۹ فایل، typecheck، lint مرتبط و production build موفق‌اند. پیش‌نمایش در `calligraphy-grid-390x844.png` ثبت شد.
- API محلی دادهٔ منتشرشده هنگام بازبینی در دسترس نبود؛ بنابراین اتصال زندهٔ پخش و ذخیره‌سازی اجرا نشد و رابط حالت آماده‌سازی را نشان داد. متن‌های معرفی پیش‌نویس و مجوز تصویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «متون کلاسیک آموزشی» — محلی، ۲۰۲۶-۰۹-۲۳

- فهرست شش مجموعهٔ مرجع 兔小贝《三字经》، 兔小贝《弟子规》، 兔小贝《千字文》، 学国学网【经典诵读·百家姓】، 中国国家图书馆《百家姓》 و 百家Talk《道德经》讲析 به‌ترتیب با ۶۲، ۳۴، ۳۱، ۵، ۴۸ و ۱۱ درس ساخته شد.
- سه کارت 兔小贝 تصویر مشترک مرجع دارند، اما در جزئیات تصاویر افقی جداگانهٔ 三字经، 弟子规 و 千字文 نمایش داده می‌شوند. برای سه مجموعهٔ دیگر نیز لوگوی فهرست و تصویر افقی مخصوص جزئیات مطابق مرجع تفکیک شدند. قالب مشترک اکنون عنوان کوتاه کارت و نسبت تصویر افقی جزئیات را پشتیبانی می‌کند.
- هر مجموعه معرفی فارسی اختصاصی، سطح، فهرست کامل درس‌ها و route مستقل هر درس دارد. عنوان بخش معرفی برای پنج برنامه «معرفی برنامه» و برای 道德经 «معرفی دوره» است. نام، مدت، امتیاز و پیشرفت نامشخص جعل نشدند.
- درس منتشرشده فقط با دورهٔ API در زیرمجموعهٔ `culture-texts`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود رکورد منتشرشده فعال می‌شود و مسیر عددی قبلی fallback باقی می‌ماند.
- بازبینی موبایل ۳۹۰×۸۴۴: شش کارت و همهٔ تصاویر سالم، شش صفحهٔ معرفی، نسبت ۱۶:۹ جزئیات، دقیقاً ۶۲/۳۴/۳۱/۵/۴۸/۱۱ لینک، درس ۶۲ و حالت «پایان مجموعه»، بدون سرریز افقی یا خطای کنسول. پیش‌نمایش در `culture-texts-grid-390x844.png` ثبت شد.

## بازبینی «فرهنگ چای» — محلی، ۲۰۲۶-۰۹-۲۳

- دورهٔ مرجع yinsong8_com（中國茶文化） با ۴۳ درس و تصویر دقیق `yinsong8_com.jpeg` ساخته شد. صفحهٔ جزئیات معرفی دانشگاهی دوره، سطح و فهرست کامل درس‌ها را نمایش می‌دهد و هر درس route مستقل دارد.
- درس منتشرشده فقط با دورهٔ API در زیرمجموعهٔ `tea-culture`، slug دقیق و `metadata_json.lesson_index` یکتا به پلیر وصل می‌شود. زمان، امتیاز و عنوان درس نامشخص ساخته نشدند و نشان‌کردن تا وجود رکورد منتشرشده غیرفعال می‌ماند.
- بازبینی موبایل ۳۹۰×۸۴۴: کارت و تصویر سالم، عنوان و زیرعنوان سنتی چینی کامل، ۴۳ لینک درس، درس ۴۳ و حالت «پایان مجموعه»، بدون سرریز افقی یا خطای کنسول. پیش‌نمایش در `tea-culture-grid-390x844.png` ثبت شد.
- مجموعهٔ کامل پس از هر دو بخش: ۱۵۱ تست در ۵۱ فایل، typecheck، lint مرتبط و production build موفق‌اند. API محلی محتوای منتشرشده در دسترس نبود؛ پخش و ذخیره‌سازی زنده آزموده نشد. متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.

## بازبینی «داستان‌های کهن» — محلی، ۲۰۲۶-۰۹-۲۳

- عنوان این زیرمجموعه در صفحهٔ اصلی کاوش و مسیر `/explore/historical-stories` از «داستان‌های تاریخی» به «داستان‌های کهن» مطابق مرجع اصلاح شد. مجموعهٔ `Chinese Tales with Xiao Lin` با تصویر دقیق موجود پروژه و ۱۸ درس برنامه‌ریزی‌شده نمایش داده می‌شود.
- صفحهٔ معرفی شامل توضیح روش داستان‌محور، سطح و فهرست کامل درس‌هاست و هر درس route مستقل دارد. عنوان، مدت، امتیاز و پیشرفت نامشخص ساخته نشدند.
- درس منتشرشده فقط با دورهٔ API در زیرمجموعهٔ `historical-stories`، slug دقیق و `metadata_json.lesson_index` یکتای همان جایگاه به پلیر متصل می‌شود. نشان‌کردن پس از وجود رکورد منتشرشده فعال می‌شود و مسیر عددی قبلی fallback باقی می‌ماند.
- بازبینی موبایل ۳۹۰×۸۴۴: کارت و تصویر سالم، صفحهٔ معرفی، دقیقاً ۱۸ لینک درس، درس ۱۸ و حالت «پایان مجموعه»، بدون سرریز افقی یا خطای کنسول. پیش‌نمایش در `historical-stories-grid-390x844.png` ثبت شد.

## بازبینی «آیین‌ها و جشن‌ها» — محلی، ۲۰۲۶-۰۹-۲۳

- مجموعهٔ `三淼儿童官方频道【精彩的传统节日】` با ۱۰ درس ساخته شد. لوگوی T-MCN در فهرست و تصویر جشن سنتی با اژدها در صفحهٔ جزئیات مطابق مرجع جدا نمایش داده می‌شوند.
- صفحهٔ معرفی شامل توضیح جشن‌ها و مناسبت‌های سنتی چین، سطح و فهرست کامل درس‌هاست و هر درس route مستقل دارد. درس واقعی فقط با زیرمجموعهٔ `festivals-customs`، slug دقیق و `metadata_json.lesson_index` یکتا به پلیر وصل می‌شود.
- بازبینی موبایل ۳۹۰×۸۴۴: کارت و هر دو تصویر سالم، عنوان و زیرعنوان کامل، ۱۰ لینک درس، درس ۱۰ و حالت «پایان مجموعه»، بدون سرریز افقی یا خطای کنسول. پیش‌نمایش در `festivals-customs-grid-390x844.png` ثبت شد.
- مجموعهٔ کامل پس از این دو بخش: ۱۵۵ تست در ۵۳ فایل، typecheck، lint مرتبط و production build موفق‌اند. API محلی محتوای منتشرشده در دسترس نبود؛ پخش و ذخیره‌سازی زنده آزموده نشد. متن‌های معرفی پیش‌نویس و مجوز تصاویر برای لانچ عمومی `review_required` است. **هیچ commit، push یا deploy** انجام نشد؛ staging تغییر نکرده است.
